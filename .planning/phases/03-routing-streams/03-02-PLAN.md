---
phase: 03-routing-streams
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docs/spec/v1/routing-modes.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - ROUTE-02
user_setup: []
must_haves:
  truths:
    - 单播 routing_mode=2 与 dst_peer_id 非零、与广播 dst=0 联合判定无歧义
    - 发往自身 peer_id 的行为为规范二选一并与 ERR 占位对齐
  artifacts:
    - docs/spec/v1/routing-modes.md
  key_links:
    - 扩展 03-01 的同文件广播小节；引用 peer-identity.md（peer_id 非 0）
---

<objective>
在 **`routing-modes.md`** 中撰写 **ROUTE-02**（私信/单播：指定目标 peer，仅目标接收），扩展路由枚举与字段规则；更新 README 索引行以包含 ROUTE-02。

Output: 更新 `docs/spec/v1/routing-modes.md`、`docs/spec/v1/README.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing-streams/03-RESEARCH.md
@docs/spec/v1/routing-modes.md
@docs/spec/v1/peer-identity.md
@docs/spec/v1/README.md
</context>

<tasks>

<task type="auto">
  <name>扩展 routing-modes.md（单播）</name>
  <files>docs/spec/v1/routing-modes.md</files>
  <read_first>docs/spec/v1/routing-modes.md, docs/spec/v1/peer-identity.md, .planning/phases/03-routing-streams/03-RESEARCH.md</read_first>
  <action>在现有 `routing-modes.md` 中增加 **单播（UNICAST）** 专节，不得删除 03-01 已冻结的广播条文。必须包含：1) **`routing_mode = 2`（`UNICAST`）** 枚举值与 **`BROADCAST = 1`** 并列说明；2) **`dst_peer_id`**：**MUST** 为 **非零** `uint64` BE，且表示会话内目标 peer；**MUST NOT** 等于发送者自身已分配 **`peer_id`**（若相等：规范 **二选一**写死——**协议错误**关闭/应答 **或** **静默丢弃**，并写明「具体错误码见 Phase 5 **ERR** 规范」）；3) **`src_peer_id`**：仍 MUST 携带发送方 **`uint64` BE**（与广播一致）；4) **与广播互斥**：**MUST** 以 **`routing_mode` + `dst_peer_id == 0` vs 非零** 联合判定（禁止仅依赖单一字段推断）；5) **Relay 行为**：单播帧 MUST 仅投递到 **`dst_peer_id`** 对应的前向连接；若目标未知/非成员/未加入：可执行句子（丢弃或错误，指向 ERR 占位）；6) 至少一段**完整帧**十六进制示例：`msg_type = 0x11`（**`STREAM_DATA`**，与 03-01 及 `streams-lifecycle.md` 一致），`routing_mode = 0x02`，`src_peer_id` 与 **`dst_peer_id` 为不同非零示例值**（大端 8 字节）；7) 在文内用 `rg` 可检字符串标注 **`<!-- REQ: ROUTE-02 -->`**（与 ROUTE-01 注释并存）。</action>
  <verify>rg -n 'UNICAST|ROUTE-02|routing_mode' docs/spec/v1/routing-modes.md</verify>
  <acceptance_criteria>
    - `rg -q 'REQ: ROUTE-02' docs/spec/v1/routing-modes.md`
    - `rg -q 'UNICAST' docs/spec/v1/routing-modes.md`
    - `rg -q 'routing_mode = 2|UNICAST = 2' docs/spec/v1/routing-modes.md`
    - `rg -q 'dst_peer_id' docs/spec/v1/routing-modes.md`
    - `rg -q 'REQ: ROUTE-01' docs/spec/v1/routing-modes.md`
    - `rg -q '0x11' docs/spec/v1/routing-modes.md`
  </acceptance_criteria>
  <done>ROUTE-02 与 ROUTE-01 在同文件内可追溯且无字段冲突</done>
</task>

<task type="auto">
  <name>更新 README 索引（ROUTE-02）</name>
  <files>docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/README.md</read_first>
  <action>将 `routing-modes.md` 对应行的说明更新为同时覆盖 **ROUTE-01** 与 **ROUTE-02**（广播与单播）。</action>
  <acceptance_criteria>
    - `rg -q 'routing-modes.md' docs/spec/v1/README.md`
    - `rg -q 'ROUTE-02' docs/spec/v1/README.md`
    - `rg -q 'ROUTE-01' docs/spec/v1/README.md`
  </acceptance_criteria>
  <done>索引与 REQ 一致</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/routing-modes.md`
- [ ] `go test ./...`
</verification>

<output>
完成后创建 `.planning/phases/03-routing-streams/03-02-SUMMARY.md`
</output>
