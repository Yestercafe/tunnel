---
phase: 03-routing-streams
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spec/v1/routing-modes.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - ROUTE-01
user_setup: []
must_haves:
  truths:
    - 广播语义（除发送者外全体成员）与 Relay 不回送发送连接为可执行规则
    - routing_mode=1、dst_peer_id=0 与 src_peer_id uint64 BE 字段在文档中有偏移表
  artifacts:
    - docs/spec/v1/routing-modes.md
  key_links:
    - 引用 frame-layout.md、peer-identity.md、session-create-join.md（数据面 opcode 与 0x01–0x04 区分）
---

<objective>
撰写 **ROUTE-01**（广播：同一 session 内除发送者外所有成员可收到）的规范，冻结数据面 payload 内 **路由前缀** 的广播语义与字段；新建 `docs/spec/v1/routing-modes.md`（本计划仅含**广播**小节，单播由 03-02 扩展同一文件），并更新 v1 索引。

Output: `docs/spec/v1/routing-modes.md`，更新 `docs/spec/v1/README.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing-streams/03-RESEARCH.md
@docs/spec/v1/frame-layout.md
@docs/spec/v1/session-create-join.md
@docs/spec/v1/peer-identity.md
@docs/spec/v1/README.md
</context>

<tasks>

<task type="auto">
  <name>新建 routing-modes.md（仅广播）</name>
  <files>docs/spec/v1/routing-modes.md</files>
  <read_first>docs/spec/v1/frame-layout.md, docs/spec/v1/session-create-join.md, docs/spec/v1/peer-identity.md, .planning/phases/03-routing-streams/03-RESEARCH.md</read_first>
  <action>新建 `docs/spec/v1/routing-modes.md`。必须包含：1) 说明数据面帧的 **payload** 仍以 **第 1 字节 `msg_type`（uint8）** 开头（与 `session-create-join.md` 一致）；**`0x01`–`0x04` 为 SESSION_*，不得复用**；**`0x10`–`0x12` 预留给流生命周期 opcode**（`STREAM_OPEN` / `STREAM_DATA` / `STREAM_CLOSE`，定义见 **`streams-lifecycle.md`** — 由后续计划撰写）。**承载「路由前缀 + 可选流字段」的数据面帧**在示例与互操作说明中 **MUST 使用 `msg_type = 0x11`（`STREAM_DATA`）**，以避免与 `0x10`（`STREAM_OPEN`）冲突。2) 紧跟 `msg_type` 之后定义**路由前缀**（固定顺序、大端）：**`routing_mode`**（`uint8`，**`BROADCAST = 1`**）、**`src_peer_id`**（`uint64` BE，发送方 peer）、**`dst_peer_id`**（`uint64` BE，广播时 **MUST 为 `0`**）；**`routing_mode = 0`** 保留为非法/未指定（规范写死实现 MUST 拒绝或丢弃，措辞与 Phase 5 ERR 占位一致即可）；3) 用**表格**列出从 payload 字节偏移 **0、1、2、10** 起的字段名与长度（`msg_type` @0，`routing_mode` @1，`src_peer_id` @2–9，`dst_peer_id` @10–17）；4) **Relay 行为**：收到意图为广播的帧时，MUST 向**同一 session 内除发送该帧的连接外**的所有已加入成员转发，且 **MUST NOT** 将副本回送给**发送者**连接；5) 至少一段**完整逻辑帧**十六进制示例（含 **10 字节**帧头 + payload）：payload 以 **`msg_type = 0x11`（STREAM_DATA）** 开头，`routing_mode = 0x01`，`src_peer_id` 填非零示例（如 `0x00000000000000ab` 的大端 8 字节），`dst_peer_id` 全 **0**；路由前缀之后的字节用注释说明「**流前缀与应用数据见 `streams-lifecycle.md`**（由后续计划撰写）」，示例中可跟 **4 字节 `stream_id` 占位 `0x00000000`** 或明确写「本例 payload 在 `dst_peer_id` 后结束」二选一，但须与帧长度自洽；6) 文末添加与 Phase 2 一致的 REQ 注释：`<!-- REQ: ROUTE-01 -->`。</action>
  <verify>rg -n 'ROUTE-01|routing_mode|BROADCAST|0x11' docs/spec/v1/routing-modes.md</verify>
  <acceptance_criteria>
    - `test -f docs/spec/v1/routing-modes.md`
    - `rg -q 'REQ: ROUTE-01' docs/spec/v1/routing-modes.md`
    - `rg -q 'routing_mode' docs/spec/v1/routing-modes.md`
    - `rg -q 'BROADCAST' docs/spec/v1/routing-modes.md`
    - `rg -q '0x11' docs/spec/v1/routing-modes.md`
    - `rg -q 'dst_peer_id' docs/spec/v1/routing-modes.md`
    - `rg -q 'MUST NOT' docs/spec/v1/routing-modes.md`
    - `rg -q 'uint64' docs/spec/v1/routing-modes.md`
  </acceptance_criteria>
  <done>ROUTE-01 可独立对照实现广播路由前缀与 Relay 不回送语义</done>
</task>

<task type="auto">
  <name>更新 README 索引</name>
  <files>docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/README.md, docs/spec/v1/routing-modes.md</read_first>
  <action>在 `docs/spec/v1/README.md` 的索引表中新增一行：`routing-modes.md`、说明含广播路由（ROUTE-01）、状态与 Phase 1/2 行风格一致。</action>
  <acceptance_criteria>
    - `rg -q 'routing-modes.md' docs/spec/v1/README.md`
    - `rg -q 'ROUTE-01' docs/spec/v1/README.md`
  </acceptance_criteria>
  <done>索引可发现新规范文件</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/routing-modes.md`
- [ ] `go test ./...`
</verification>

<output>
完成后创建 `.planning/phases/03-routing-streams/03-01-SUMMARY.md`
</output>
