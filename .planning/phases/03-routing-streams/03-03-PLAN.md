---
phase: 03-routing-streams
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - docs/spec/v1/streams-lifecycle.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - STREAM-01
  - STREAM-02
user_setup: []
must_haves:
  truths:
    - 数据面 opcode 表从 0x10 起与 SESSION_* 无冲突；OPEN/DATA/CLOSE（及可选 FIN）语义单一、无分裂
    - stream_id uint32 BE、连接内唯一；同一 stream_id 上 DATA 应用字节序有序，流间不要求顺序
  artifacts:
    - docs/spec/v1/streams-lifecycle.md
  key_links:
    - 引用 routing-modes.md 中「msg_type + routing_mode + src_peer_id + dst_peer_id」偏移与长度，再接 stream 前缀
---

<objective>
撰写 **STREAM-01**（双向流：单 peer–Relay 连接上多逻辑流可持续收发）与 **STREAM-02**（`stream_id` 语义、流内有序、流间乱序）的规范；新建 `docs/spec/v1/streams-lifecycle.md`，与 `routing-modes.md` 中的路由前缀**字节级衔接**；更新 README。

Output: `docs/spec/v1/streams-lifecycle.md`，更新 `docs/spec/v1/README.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing-streams/03-RESEARCH.md
@docs/spec/v1/routing-modes.md
@docs/spec/v1/session-create-join.md
@docs/spec/v1/frame-layout.md
@docs/spec/v1/README.md
</context>

<tasks>

<task type="auto">
  <name>撰写 streams-lifecycle.md</name>
  <files>docs/spec/v1/streams-lifecycle.md</files>
  <read_first>docs/spec/v1/routing-modes.md, docs/spec/v1/session-create-join.md, docs/spec/v1/frame-layout.md, .planning/phases/03-routing-streams/03-RESEARCH.md</read_first>
  <action>新建 `docs/spec/v1/streams-lifecycle.md`。必须包含：1) **数据面 `msg_type` opcode 表**（与 `0x01`–`0x04` 无重叠）：**`0x10` = `STREAM_OPEN`**、**`0x11` = `STREAM_DATA`**、**`0x12` = `STREAM_CLOSE`**（若规范采用 **FIN 位**，须在 **`STREAM_DATA`** 的 flags 字节中定义单 bit，并与 CLOSE 关系写清；**三选一组合须只选一种关闭路径**，避免实现分裂——推荐同时定义 **CLOSE** 与可选 **DATA+FIN** 时须写明优先级）；2) **payload 布局**（在 [routing-modes.md](./routing-modes.md) 已定义的 **18 字节**「`msg_type` + `routing_mode` + `src_peer_id` + `dst_peer_id`」之后）：**`stream_id`**（`uint32` BE）、若 OPEN 含 **可选元数据**则 **`metadata_len`（`uint16` BE）+ `metadata` 字节**（可为 0 长度）；**`STREAM_DATA`** 在 `stream_id` 后定义 **`flags`（`uint8`）** 与 **`payload_len`（`uint16` BE）+ 应用字节**（或采用「剩余 payload 即数据」——须二选一写死）；**`STREAM_CLOSE`** 体至少含 **`stream_id`（`uint32` BE）**；3) **`stream_id` 规则**：**`uint32` BE**；**仅在本 TLS 连接上唯一**；**`stream_id = 0`**：**禁止**作为有效数据流 ID（MUST NOT 出现在 OPEN/DATA 的有效流中）或保留给「无流」——**选一种写死**；4) **STREAM-01 双向流**：同一 `stream_id` 上双方可交替发送 **`STREAM_DATA`** 直至关闭；**不在** `SESSION_*` 消息中携带 `stream_id`；5) **STREAM-02 顺序**：明文 **「对同一 `(connection, stream_id)` 上 `STREAM_DATA` 承载的应用字节序列，接收方处理顺序 MUST 与发送顺序一致」**；**「不同 `stream_id` 的帧 MAY 任意交错；接收方 MUST NOT 假定流 A 的某 DATA 早于流 B 的某 DATA」**；并写明与 **TLS 字节流线序**的关系（乱序指逻辑多路，非 TCP 乱序）；6) **并发多流文字用例**（非代码）：例如 peer A 向 B 同时 `stream_id=3` 传文件块与 `stream_id=4` 传心跳，B 侧观测交错但各流内字节序不变；7) 偏移汇总表：从 payload 起点列出 **`msg_type` @0、`routing_mode` @1、`src` @2、`dst` @10、`stream_id` @18**（若与 OPEN 元数据布局有额外字节，表须更新为一致）；8) 文末 **`<!-- REQ: STREAM-01 -->`** 与 **`<!-- REQ: STREAM-02 -->`**。</action>
  <verify>rg -n 'STREAM-0|0x10|0x11|0x12|stream_id' docs/spec/v1/streams-lifecycle.md</verify>
  <acceptance_criteria>
    - `test -f docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'REQ: STREAM-01' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'REQ: STREAM-02' docs/spec/v1/streams-lifecycle.md`
    - `rg -q '0x10' docs/spec/v1/streams-lifecycle.md`
    - `rg -q '0x11' docs/spec/v1/streams-lifecycle.md`
    - `rg -q '0x12' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'stream_id' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'uint32' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'routing-modes.md' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'MUST NOT' docs/spec/v1/streams-lifecycle.md`
  </acceptance_criteria>
  <done>STREAM-01/02 与路由前缀可拼接解析</done>
</task>

<task type="auto">
  <name>更新 README 索引</name>
  <files>docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/README.md</read_first>
  <action>在 `docs/spec/v1/README.md` 索引表增加 **`streams-lifecycle.md`** 一行，标明 **STREAM-01、STREAM-02**，状态与现有表一致。</action>
  <acceptance_criteria>
    - `rg -q 'streams-lifecycle.md' docs/spec/v1/README.md`
    - `rg -q 'STREAM-01' docs/spec/v1/README.md`
    - `rg -q 'STREAM-02' docs/spec/v1/README.md`
  </acceptance_criteria>
  <done>索引完整</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/streams-lifecycle.md`
- [ ] `rg 'ROUTE-0|STREAM-0' docs/spec/v1/`
- [ ] `go test ./...`
</verification>

<output>
完成后创建 `.planning/phases/03-routing-streams/03-03-SUMMARY.md`
</output>
