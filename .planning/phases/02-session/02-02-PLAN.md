---
phase: 02-session
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - docs/spec/v1/peer-identity.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - SESS-03
user_setup: []
must_haves:
  truths:
    - peer_id 宽度、字节序与分配规则明确
    - 与 session-create-join 中 JOIN 响应一致
  artifacts:
    - docs/spec/v1/peer-identity.md
  key_links:
    - 引用 session-create-join.md
---

<objective>
撰写 **SESS-03**：**peer_id** 的分配、唯一性范围与在协议中的引用方式（为 Phase 3 路由预留）。

Output: `docs/spec/v1/peer-identity.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/spec/v1/session-create-join.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>撰写 peer 身份规范</name>
  <files>docs/spec/v1/peer-identity.md, docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/session-create-join.md</read_first>
  <action>新建 `docs/spec/v1/peer-identity.md`。必须包含：1) **peer_id** 类型 **uint64**，大端，**0** 保留；2) **分配者**：Relay，在 **JOIN 成功** 后分配，**会话内唯一**；3) 与 **连接** 的绑定关系（同一 TLS 连接上的 peer 在同一 session 内一个 peer_id）；4) **在后续帧中的引用**（预告：Phase 3 路由头将携带 `peer_id`/`dst_peer_id`，本文件只定义语义）；5) 文末 **SESS-03**。更新 `README.md`。</action>
  <verify>rg -q 'SESS-03' docs/spec/v1/peer-identity.md</verify>
  <acceptance_criteria>
    - `peer-identity.md` 包含 `SESS-03` 与 `uint64`
  </acceptance_criteria>
  <done>SESS-03 无歧义</done>
</task>

</tasks>

<output>
完成后创建 `.planning/phases/02-session/02-02-SUMMARY.md`
</output>
