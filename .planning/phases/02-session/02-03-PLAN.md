---
phase: 02-session
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - docs/spec/v1/join-credentials.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - SESS-04
user_setup: []
must_haves:
  truths:
    - token 携带位置与失败行为可编码
    - 与 ERR 占位符名称一致
  artifacts:
    - docs/spec/v1/join-credentials.md
  key_links:
    - 引用 session-create-join 中 JOIN
---

<objective>
撰写 **SESS-04**：可选 **join token** 的格式、在消息中的位置及校验失败时的行为（占位错误名）。

Output: `docs/spec/v1/join-credentials.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/spec/v1/session-create-join.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>撰写 join 凭证规范</name>
  <files>docs/spec/v1/join-credentials.md, docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/session-create-join.md</read_first>
  <action>新建 `docs/spec/v1/join-credentials.md`。必须包含：1) **何时需要 token**（由创建 session 时策略决定，文档说明为**可选**）；2) **token** 在 **JOIN** 消息体中的字段名与最大长度（UTF-8 字节）；3) 校验失败：**`ERR_JOIN_DENIED`**；session 不存在：**`ERR_SESSION_NOT_FOUND`**（占位，与 Phase 5 对齐）；4) 成功路径不重复，引用 session-create-join；5) 文末 **SESS-04**。更新 `README.md`。</action>
  <verify>rg -q 'SESS-04' docs/spec/v1/join-credentials.md</verify>
  <acceptance_criteria>
    - 文件包含 `SESS-04` 与 `ERR_JOIN_DENIED`
  </acceptance_criteria>
  <done>SESS-04 完成</done>
</task>

</tasks>

<output>
完成后创建 `.planning/phases/02-session/02-03-SUMMARY.md`
</output>
