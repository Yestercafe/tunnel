---
phase: 02-session
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spec/v1/session-create-join.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - SESS-01
  - SESS-02
user_setup: []
must_haves:
  truths:
    - session_id 与邀请码格式可独立实现
    - 创建/加入消息类型与最小往返序列可画图
  artifacts:
    - docs/spec/v1/session-create-join.md
  key_links:
    - 引用 frame-layout.md 的 payload 定义
---

<objective>
撰写 **SESS-01**（创建 session 与 session_id/邀请码）与 **SESS-02**（凭 session_id/邀请码加入）的规范，并更新 v1 索引。

Output: `docs/spec/v1/session-create-join.md`，更新 `docs/spec/v1/README.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@docs/spec/v1/frame-layout.md
@.planning/phases/02-session/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>撰写 session 创建与加入规范</name>
  <files>docs/spec/v1/session-create-join.md, docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/frame-layout.md, .planning/REQUIREMENTS.md, .planning/phases/02-session/02-RESEARCH.md</read_first>
  <action>新建 `docs/spec/v1/session-create-join.md`。必须包含：1) **payload 内控制消息** 与 Phase 1 帧的关系（payload 为变长字节，本规范定义其 **msg_type** 与体结构）；2) **session_id** 格式（建议 UUID 字符串 UTF-8，给出正则或长度约束）；3) **invite_code** 格式（Base32、长度范围）；4) **opcode 表**（至少：创建请求/响应、加入请求/响应），大端/字节序与字段表；5) **Relay 最小成员表** 推导（字段列表：session_id、peer_id、…）；6) **ASCII 序列图** 或编号步骤描述「创建者开房 → peer 加入」；7) 文末标注 **SESS-01**、**SESS-02**。更新 `README.md` 增加本文件行且状态为已发布或草案。</action>
  <verify>rg -q 'SESS-01' docs/spec/v1/session-create-join.md</verify>
  <acceptance_criteria>
    - 文件同时包含字符串 `SESS-01` 与 `SESS-02`
    - `README.md` 含 `session-create-join.md`
  </acceptance_criteria>
  <done>SESS-01/02 可被 Relay 实现者直接对照</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/session-create-join.md`
</verification>

<output>
完成后创建 `.planning/phases/02-session/02-01-SUMMARY.md`
</output>
