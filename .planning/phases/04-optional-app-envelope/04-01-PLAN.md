---
phase: 04-optional-app-envelope
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spec/v1/app-envelope.md
  - docs/spec/v1/streams-lifecycle.md
  - docs/spec/v1/README.md
  - pkg/appenvelope/split.go
  - pkg/appenvelope/split_test.go
autonomous: true
requirements:
  - APP-01
user_setup: []
must_haves:
  truths:
    - flags 字节 @payload 内偏移 22 明确定义 bit0=FIN、bit1=HAS_APP_ENVELOPE，且与 Phase 3 无信封路径兼容
    - STREAM_DATA 的 application_data @25 在 HAS_APP_ENVELOPE=1 时分解为 envelope_len(uint16 BE @25–26)+envelope+body
    - v1 互操作信封体编码选定一种（本计划为 UTF-8 JSON）并写清键名与最大 envelope_len 规则
    - Go 可对无信封与有信封两种 application_data 字节切片做边界拆分并通过表驱动测试
  artifacts:
    - path: docs/spec/v1/app-envelope.md
      provides: APP-01 正文、JSON 模式、偏移与 REQ 注释
    - path: docs/spec/v1/streams-lifecycle.md
      provides: STREAM_DATA flags/application_data 行与 app-envelope 交叉引用
    - path: pkg/appenvelope/split.go
      provides: 与规范一致的 application_data 切分辅助
  key_links:
    - from: docs/spec/v1/streams-lifecycle.md
      to: docs/spec/v1/app-envelope.md
      via: 「应用信封详见」链接
    - from: pkg/appenvelope/split.go
      to: docs/spec/v1/app-envelope.md
      via: 包注释引用 APP-01 与 flags 位常量
---

<objective>
冻结 **APP-01**：在 Phase 3 已发布的 **STREAM_DATA** 布局上增加**可选应用信封**；**选定 v1 唯一互操作编码为 UTF-8 JSON**（明确放弃 TLV 作为 v1 必选互操作格式，TLV 可作为实现私有扩展留在文外说明一句即可）；扩展 **`flags` 第 1 位 `HAS_APP_ENVELOPE`**（**第 0 位仍为 `FIN`**）；当 `HAS_APP_ENVELOPE=1` 时，`application_data`（总长仍为 `payload_len`）= **`envelope_len`（uint16 BE，占 2 字节）** + **`envelope`（`envelope_len` 字节）** + **`body`（剩余字节）**；新增 `docs/spec/v1/app-envelope.md`；修订 `streams-lifecycle.md` 与 `README.md`；实现 **`pkg/appenvelope`** 最小切分逻辑与测试，供后续示例与 Phase 6 向量复用。

Purpose: 无信封与旧语义一致；有信封时 HTTP 式与 Copilot 式元数据可共享同一隧道。
Output: 上述规范文件、`pkg/appenvelope`、通过 `go test ./...`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-optional-app-envelope/04-RESEARCH.md
@docs/spec/v1/streams-lifecycle.md
@docs/spec/v1/routing-modes.md
@docs/spec/v1/README.md
</context>

<interfaces>
<!-- 现有 STREAM_DATA 字段偏移（相对 payload 起点）— 执行器不得改动 @18 stream_id 之前字节 -->

| 字段 | 偏移（字节） | 长度 |
|------|----------------|------|
| `stream_id` | 18 | 4 |
| `flags` | 22 | 1 |
| `payload_len` | 23 | 2 |
| `application_data` | 25 | `payload_len` |

**位定义（本计划后）：** `flags` bit **0** = `FIN`；bit **1** = `HAS_APP_ENVELOPE`；bit **2–7** 保留（发送 **MUST** 为 0；接收 **MUST** 忽略未知位）。
</interfaces>

<tasks>

<task type="auto">
  <name>新建 app-envelope.md（选定 JSON、字段与边界）</name>
  <files>docs/spec/v1/app-envelope.md</files>
  <read_first>docs/spec/v1/streams-lifecycle.md, docs/spec/v1/routing-modes.md, .planning/phases/04-optional-app-envelope/04-RESEARCH.md</read_first>
  <action>新建 `docs/spec/v1/app-envelope.md`。必须包含：1) **编码决策（择一）**：声明 v1 互操作 **`envelope` 字节**为 **UTF-8 编码的 JSON 对象**（**不**采用 TLV 作为 v1 规范必选格式；若需 TLV 仅为实现私有约定，**不得**标为 v1 互操作）。2) **JSON schema 子集**：至少三个键 **`content_type`**、**request_id`**、**correlation_id`**，值类型均为 **UTF-8 字符串**（允许 `""`）；禁止嵌套对象/数组（或写死「仅扁平字符串键」）。3) **`envelope_len` 上限**：给出具体数字（例如 **4096**，且 **≤** 帧级约束），并说明超过上限为协议/解码错误（**ERR_*** 指向 Phase 5 占位）。4) **与 `application_data` 分解**：相对 **payload 起点**，`application_data` 起始于 **@25**；当 **`HAS_APP_ENVELOPE=0`** 时，**@25 起共 `payload_len` 字节**均为 **body**（与 Phase 3  opaque 一致，**无** `envelope_len` 前缀）。当 **`HAS_APP_ENVELOPE=1`** 时：**@25–26** = `envelope_len`（**uint16 BE**）；**@27 .. @26+envelope_len`** = `envelope`；其后至本段末尾为 **body**；约束 **`2 + envelope_len ≤ payload_len`**，否则错误。5) **Relay 不透明**：重申 Relay **不解析** JSON，仅转发帧（与 ROUTE 一致）。6) 文末 HTML 注释：`<!-- REQ: APP-01 -->`。</action>
  <verify>rg -n 'APP-01|HAS_APP_ENVELOPE|envelope_len|application/json|content_type' docs/spec/v1/app-envelope.md</verify>
  <acceptance_criteria>
    - `test -f docs/spec/v1/app-envelope.md`
    - `rg -q 'REQ: APP-01' docs/spec/v1/app-envelope.md`
    - `rg -q 'HAS_APP_ENVELOPE' docs/spec/v1/app-envelope.md`
    - `rg -q '@25' docs/spec/v1/app-envelope.md`
    - `rg -q 'uint16' docs/spec/v1/app-envelope.md`
    - `rg -q 'bit 1' docs/spec/v1/app-envelope.md`
    - `rg -q 'content_type' docs/spec/v1/app-envelope.md`
    - `rg -q 'request_id' docs/spec/v1/app-envelope.md`
    - `rg -q 'correlation_id' docs/spec/v1/app-envelope.md`
    - `rg -q 'JSON' docs/spec/v1/app-envelope.md`
    - `rg -q 'TLV' docs/spec/v1/app-envelope.md`
  </acceptance_criteria>
  <done>APP-01 规范单文件可独立阅读；字节偏移与 JSON 键名可被实现直接对照</done>
</task>

<task type="auto">
  <name>修订 streams-lifecycle.md 与 README 索引</name>
  <files>docs/spec/v1/streams-lifecycle.md, docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/app-envelope.md, docs/spec/v1/streams-lifecycle.md, docs/spec/v1/README.md</read_first>
  <action>1) 在 `docs/spec/v1/streams-lifecycle.md` 的 **`STREAM_DATA`** 小节：将 **`flags` @22** 行改为明确 **bit 0 = `FIN`**、**bit 1 = `HAS_APP_ENVELOPE`**、**bit 2–7** 保留（发送 MUST 0、接收 MUST 忽略未知位）。2) 将 **`application_data` @25** 说明改为：总长 **`payload_len`**；语义分岔见 **`app-envelope.md`**（**`HAS_APP_ENVELOPE=0`** 时整段为应用数据；**`=1`** 时为 `envelope_len`+`envelope`+`body`）。3) 若存在 **偏移汇总**表，同步 **flags** 语义一行，不改动 **数值偏移**（**25** 仍为 `application_data` 起点）。4) 在 `docs/spec/v1/README.md` 索引表新增 **`app-envelope.md`** 一行（**APP-01**、已发布/草案风格与现有行一致）。</action>
  <verify>rg -n 'HAS_APP_ENVELOPE|app-envelope.md|bit 1' docs/spec/v1/streams-lifecycle.md docs/spec/v1/README.md</verify>
  <acceptance_criteria>
    - `rg -q 'HAS_APP_ENVELOPE' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'bit 0' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'app-envelope.md' docs/spec/v1/streams-lifecycle.md`
    - `rg -q 'app-envelope.md' docs/spec/v1/README.md`
    - `rg -q 'APP-01' docs/spec/v1/README.md`
  </acceptance_criteria>
  <done>STREAM_DATA 与索引读者能定位到应用信封专篇且无偏移冲突</done>
</task>

<task type="auto" tdd="true">
  <name>pkg/appenvelope：按 flags 切分 application_data</name>
  <files>pkg/appenvelope/split.go, pkg/appenvelope/split_test.go</files>
  <read_first>docs/spec/v1/app-envelope.md, pkg/framing/decode.go</read_first>
  <behavior>
    - `flags=0x00`、`application_data=[1,2,3]` → body 全长 `[1,2,3]`，无信封前缀
    - `flags=0x02`（仅 HAS_APP_ENVELOPE）、`application_data` 长度不足 2 → 返回错误
    - `flags=0x02`、`envelope_len=0` → envelope 为空字节切片，body 为剩余全部
    - `flags=0x02`、`2+envelope_len > len(application_data)` → 错误
    - `flags=0x03`（FIN|HAS_APP_ENVELOPE）与 `0x02` 切分行为一致（仅测试拆分，不解释 FIN 语义）
  </behavior>
  <action>新增包 **`pkg/appenvelope`**：1) 导出常量 **`FlagFIN = 1 << 0`**、**`FlagHasAppEnvelope = 1 << 1`**（与 `streams-lifecycle.md` / `app-envelope.md` 一致）。2) 实现 **`SplitApplicationData(flags uint8, applicationData []byte) (envelope []byte, body []byte, err error)`**：若 **`flags & FlagHasAppEnvelope == 0`**，返回 **`envelope=nil`**（或零长切片二选一文内统一）、**`body=applicationData`**、**`err=nil`**；若置位则读 **`binary.BigEndian.Uint16(applicationData[0:2])`** 为 `n`，校验长度后 **`envelope = applicationData[2 : 2+n]`**，**`body = applicationData[2+n:]`**。3) 包注释引用 **`docs/spec/v1/app-envelope.md`** 与 **APP-01**。4) **不**在此包内 `encoding/json` 解码 envelope（仅字节边界；JSON 语义属上层）。错误变量使用 **`errors.New` 小写哨兵**或 **`fmt.Errorf`**，表驱动测试覆盖 **behavior** 中各条。</action>
  <verify>go test ./pkg/appenvelope/... -count=1 -v</verify>
  <acceptance_criteria>
    - `test -f pkg/appenvelope/split.go`
    - `test -f pkg/appenvelope/split_test.go`
    - `rg -q 'FlagHasAppEnvelope' pkg/appenvelope/split.go`
    - `rg -q 'SplitApplicationData' pkg/appenvelope/split.go`
    - `go test ./... -count=1`
    - `rg -n 'APP-01|app-envelope' pkg/appenvelope/split.go`
  </acceptance_criteria>
  <done>实现与规范字节分解一致；全仓库测试通过</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/app-envelope.md`
- [ ] `rg -q 'REQ: APP-01' docs/spec/v1/app-envelope.md`
- [ ] `rg -q 'HAS_APP_ENVELOPE' docs/spec/v1/streams-lifecycle.md`
- [ ] `rg -q 'app-envelope.md' docs/spec/v1/README.md`
- [ ] `go test ./... -count=1`
</verification>

<output>
完成后创建 `.planning/phases/04-optional-app-envelope/04-01-SUMMARY.md`
</output>
