---
phase: 04-optional-app-envelope
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - docs/spec/v1/app-envelope.md
autonomous: true
requirements:
  - APP-01
user_setup: []
must_haves:
  truths:
    - 文档含至少两个端到端示例：JSON 请求/响应式（content-type + request id）；Copilot 往返式（correlation id 关联两帧）
    - 每个示例含十六进制或字节偏移表，可 grep 到固定魔数/键名以便回归
  artifacts:
    - path: docs/spec/v1/app-envelope.md
      provides: 「示例」章节含两则场景与分解表
  key_links:
    - from: 示例章节
      to: STREAM_DATA @22/@25 字段
      via: 明确同一 stream_id 或两帧对照说明
---

<objective>
在 **`04-01`** 已冻结的布局与 JSON 信封键名之上，于 **`docs/spec/v1/app-envelope.md`** 增补**至少两个端到端示例**（**Roadmap 成功标准 2**）：(1) **JSON 请求/响应风格**：`content_type`（如 `application/json`）与 **`request_id`** 配对；(2) **Copilot 往返**：两帧 **`STREAM_DATA`** 共享 **`correlation_id`**，并用 **`request_id`** 区分分支。示例须含 **十六进制片段或偏移表**（从 **payload** 或 **`application_data` @25** 起算清晰标注）。

Purpose: 使互操作实现与测试向量可从规范直接抄写。
Output: 更新后的 **`app-envelope.md`**（示例章节）；无新 Go 包文件则验证以 **rg / test -f** 为主，仍运行全量 **`go test ./...`** 确保无破坏。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/spec/v1/app-envelope.md
@docs/spec/v1/streams-lifecycle.md
@docs/spec/v1/routing-modes.md
@.planning/phases/04-optional-app-envelope/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>示例 A：JSON 请求/响应式（content-type 与 request id）</name>
  <files>docs/spec/v1/app-envelope.md</files>
  <read_first>docs/spec/v1/app-envelope.md, docs/spec/v1/streams-lifecycle.md</read_first>
  <action>在 `docs/spec/v1/app-envelope.md` 新增章节（例如 **「端到端示例 A：JSON 请求/响应」**）。必须包含：1) 文字说明：一帧 **`STREAM_DATA`**，**`HAS_APP_ENVELOPE=1`**，**body** 为 JSON 负载（可最小如 `{"ok":true}` 的 UTF-8）；信封 JSON 含 **`content_type":"application/json"`** 与 **`request_id`**（非空示例字符串，如 **`req-7a2f`**）；可选 **`correlation_id`** 为空或省略。2) **响应**语义：第二帧可简述为「同一 **`request_id`** 应答」或成对 **`request_id`**，须在正文二选一拍板并自洽。3) **字节可验证性**：给出 **至少 8 个连续十六进制字节** 覆盖 **`application_data` 起始**（应含 **`envelope_len` 大端** 的前两字节与 JSON 开头可见 ASCII），或给出 **从 payload @25 起的偏移表**（列：偏移、长度、含义）。4) 标明 **`flags`** 字节值（含 **bit1**，例如 **`0x02`** 无 FIN）。</action>
  <verify>rg -n '示例 A|request_id|application/json|0x02' docs/spec/v1/app-envelope.md</verify>
  <acceptance_criteria>
    - `rg -q '示例' docs/spec/v1/app-envelope.md`
    - `rg -q 'request_id' docs/spec/v1/app-envelope.md`
    - `rg -q 'application/json' docs/spec/v1/app-envelope.md`
    - `rg -q '0x02' docs/spec/v1/app-envelope.md`
    - `rg -E '([0-9a-fA-F]{2} ){7}[0-9a-fA-F]{2}' docs/spec/v1/app-envelope.md`
    - `rg -q '@25' docs/spec/v1/app-envelope.md`
  </acceptance_criteria>
  <done>读者可从规范复制十六进制/表构造一帧含 JSON 信封的请求（及所述响应关系）</done>
</task>

<task type="auto">
  <name>示例 B：Copilot 往返（correlation id 关联两帧）</name>
  <files>docs/spec/v1/app-envelope.md</files>
  <read_first>docs/spec/v1/app-envelope.md, docs/spec/v1/routing-modes.md</read_first>
  <action>在同一文件追加章节（例如 **「端到端示例 B：Copilot 往返」**）。必须包含：1) **Peer A → Peer B** 与 **B → A** 两帧 **`STREAM_DATA`**（可同 **`stream_id`** 或说明双向流上两方向；**路由前缀**须合法：例如 **`UNICAST`** + 明确 **`src_peer_id`/`dst_peer_id`** 或引用 `routing-modes.md` 表格不手写满帧若篇幅受限，但 **至少**写清 **`msg_type=0x11`**、**`routing_mode`**、**`stream_id`** 字段意图）。2) 两帧信封 JSON **共享同一非空 `correlation_id`**（示例值如 **`corr-copilot-01`**）；**`request_id`** 在两帧 **不同** 以区分调用分支。3) **分解**：每帧给出 **表格或十六进制**（**`flags` @payload+22**、**`payload_len` @23–24**、**`application_data` @25+** 的 `envelope_len` + envelope + body 前缀字节）。4) 一句声明：**Relay 不解释信封**，与 **04-01** 正文一致。</action>
  <verify>rg -n '示例 B|correlation_id|corr-copilot|Relay' docs/spec/v1/app-envelope.md</verify>
  <acceptance_criteria>
    - `rg -q 'correlation_id' docs/spec/v1/app-envelope.md`
    - `rg -q 'Copilot' docs/spec/v1/app-envelope.md`
    - `rg -q 'STREAM_DATA' docs/spec/v1/app-envelope.md`
    - `rg -q '0x11' docs/spec/v1/app-envelope.md`
    - `rg -q 'Relay' docs/spec/v1/app-envelope.md`
    - `rg -E '([0-9a-fA-F]{2} ){7}[0-9a-fA-F]{2}' docs/spec/v1/app-envelope.md`
  </acceptance_criteria>
  <done>两帧关联关系与 correlation/request 正交性有可读、可 grep 的实例</done>
</task>

</tasks>

<verification>
- [ ] `rg -q '示例 A' docs/spec/v1/app-envelope.md`
- [ ] `rg -q '示例 B' docs/spec/v1/app-envelope.md`
- [ ] `go test ./... -count=1`
</verification>

<output>
完成后创建 `.planning/phases/04-optional-app-envelope/04-02-SUMMARY.md`
</output>

## PLANNING COMPLETE
