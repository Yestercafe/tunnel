---
phase: 07-pkg-protocol
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - pkg/protocol/join_gate.go
  - pkg/protocol/join_gate_test.go
  - pkg/protocol/doc.go
autonomous: true
requirements:
  - PROT-02
user_setup: []
must_haves:
  truths:
    - 在 **未加入**（`joined==false`）时，对完整 payload 首字节 `msg_type ∈ {0x10,0x11,0x12}` 的判定为「**非合法数据面业务路径**」；`joined==true` 时不因此拒绝
    - `msg_type==0x05`（`PROTOCOL_ERROR`）**不**视为数据面路由帧（与 `07-RESEARCH.md` / `session-state.md` STATE-01 一致）
    - Client/Relay 可复用同一包内导出函数，无需复制 opcode 表
  artifacts:
    - path: pkg/protocol/join_gate.go
      provides: "JOIN_ACK 前数据面门禁（PROT-02）"
      min_lines: 25
    - path: pkg/protocol/join_gate_test.go
      provides: "门禁矩阵单测（含 JoinGate 子测试名）"
      min_lines: 40
    - path: pkg/protocol/doc.go
      provides: "包文档注释引用 PROT-02 与 session-state.md"
      min_lines: 8
  key_links:
    - from: pkg/protocol/join_gate.go
      to: pkg/protocol/msgtype.go
      via: "数据面 opcode 与 MsgTypeStream* 常量一致"
      pattern: "0x10|MsgTypeStream"
    - from: pkg/protocol/join_gate.go
      to: docs/spec/v1/session-state.md
      via: "STATE-01：JOIN_ACK 前不得发送数据面路由帧"
      pattern: "joined|Join"
---

<objective>
实现 **PROT-02**：**`join_gate`** 辅助函数 — 在收到 **`SESSION_JOIN_ACK` 且 `peer_id` 有效之前**，不得将 **`msg_type` 为 `0x10`/`0x11`/`0x12`** 的载荷视为合法数据面业务路径；补充包级文档与门禁矩阵测试。

Purpose: 满足 ROADMAP Phase 7 成功标准第 2 条（共享 JOIN 前判定逻辑）。
Output: `join_gate.go`、`join_gate_test.go`、`doc.go`；`go test ./pkg/protocol/...` 与 `go test ./...` 通过。
</objective>

<execution_context>
@/Users/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pkg-protocol/07-RESEARCH.md
@docs/spec/v1/session-state.md
@pkg/protocol/msgtype.go

<interfaces>
<!-- 依赖 07-01 导出的 msg_type 常量；门禁逻辑不解析路由前缀字节，仅检视首字节（完整 payload 已由成帧层交付）。 -->
```go
// session-state.md STATE-01：未收到 SESSION_JOIN_ACK 且 peer_id 有效前，MUST NOT 发送带数据面路由前缀的帧。
// 07-RESEARCH：msg_type ∈ {0x10,0x11,0x12} 为数据面流 opcode；PROTOCOL_ERROR 0x05 不算数据面。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: join_gate 辅助函数与门禁矩阵测试</name>
  <files>pkg/protocol/join_gate.go, pkg/protocol/join_gate_test.go</files>
  <read_first>docs/spec/v1/session-state.md, .planning/phases/07-pkg-protocol/07-RESEARCH.md, pkg/protocol/msgtype.go</read_first>
  <behavior>
    - `joined=false` 且 `payload[0]∈{0x10,0x11,0x12}` → **阻塞**（非合法数据面业务路径）
    - `joined=false` 且 `payload[0]∈{0x01,0x02,0x03,0x04}` → **不**按数据面阻塞（控制面）
    - `joined=false` 且 `payload[0]==0x05`（`PROTOCOL_ERROR`）→ **不**按「数据面 STREAM_*」阻塞（与 `0x11` 区分）
    - `joined=true` 且 `payload[0]∈{0x10,0x11,0x12}` → 允许进入数据面业务路径判定（`ok=true`）
    - `len(payload)==0` → 返回 **`error`**（非成功）
  </behavior>
  <action>
    新建 `join_gate.go`：导出至少一个函数，例如 `func JoinGateAllowsBusinessDataPlane(joined bool, payload []byte) (ok bool, err error)` 或 `func DataPlaneBlockedBeforeJoinAck(joined bool, msgType byte) bool`（若仅传 `msgType`，则文档说明 **调用方必须在完整 payload 上取首字节**）。**推荐签名**：接收 **完整 payload**，空切片返回 **`error`**（非静默成功）。实现规则：**当 `!joined` 且首字节为 `0x10`、`0x11` 或 `0x12` 时**，返回「不允许作为合法数据面业务路径」（`ok=false` 或 `blocked=true`）；**`0x05` 不**触发数据面拦截（与 `PROTOCOL_ERROR` 控制面一致）。

    `join_gate_test.go`：文件内测试名包含 **`JoinGate`**（满足 `-run JoinGate`）；矩阵覆盖：`joined` × `msg_type`（至少 `0x01`–`0x05`、`0x10`–`0x12`）；显式断言 **`0x05` 在 `joined=false` 时不被当作数据面阻塞**（与 `0x11` 区分）。
  </action>
  <verify>
    <automated>go test ./pkg/protocol/... -run JoinGate -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/protocol/... -run JoinGate -count=1` 退出码 0
    - `rg -n 'JoinGate|0x10|0x05' pkg/protocol/join_gate_test.go`
    - `rg -n 'func ' pkg/protocol/join_gate.go`
  </acceptance_criteria>
  <done>PROT-02 门禁行为可测且与 session-state.md / opcode 表一致</done>
</task>

<task type="auto">
  <name>Task 2: 包文档与全量测试门禁</name>
  <files>pkg/protocol/doc.go</files>
  <read_first>pkg/protocol/join_gate.go, .planning/ROADMAP.md</read_first>
  <action>
    添加 `doc.go`：`package protocol` 注释说明本包为 **payload 语义层**（不解析 `pkg/framing` 10 字节头）；引用 **`PROT-01` / `PROT-02`** 与 `docs/spec/v1/session-state.md` STATE-01；说明 `join_gate` 用于 JOIN_ACK 前数据面拦截。

    运行并修复直至通过：**`go test ./pkg/protocol/... -count=1`** 与 **`go test ./... -count=1`**。
  </action>
  <verify>
    <automated>go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` 退出码 0
    - `rg -n 'PROT-02|session-state|STATE-01' pkg/protocol/doc.go`
  </acceptance_criteria>
  <done>仓库全量测试绿；包文档可追溯需求与规范</done>
</task>

</tasks>

<verification>
`go test ./pkg/protocol/... -run JoinGate -count=1`；`go test ./... -count=1`。
</verification>

<success_criteria>
- ROADMAP Phase 7 成功标准 **#2** 满足：**JOIN_ACK 前**共享逻辑将带数据面 `STREAM_*` opcode 的帧视为非合法业务路径；**`PROTOCOL_ERROR` 不误判**。
</success_criteria>

<output>
完成后创建 `.planning/phases/07-pkg-protocol/07-02-SUMMARY.md`。
</output>

---

## PLANNING COMPLETE
