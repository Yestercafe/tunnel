---
phase: 08-client-pkg-client-cmd
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - cmd/tunnel/main.go
  - cmd/tunnel/client.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Binary exposes client subcommands with --addr and explicit --insecure-skip-verify (default false)."
    - "TLS default uses system roots when insecure flag is false; insecure path sets tls.Config.InsecureSkipVerify only via flag."
  artifacts:
    - path: cmd/tunnel/main.go
      provides: "Single main, subcommand dispatch"
    - path: cmd/tunnel/client.go
      provides: "Flags and pkg/client.Dial calls for smoke"
---

<objective>
Add **`cmd/tunnel`**: one **`main` package** (D-05) with **client** subcommands for **manual smoke** — e.g. **`tunnel client create`** and **`tunnel client join`** (exact literals per D-05 / executor discretion). Wire **`flag`** or **`spf13/pflag`** only if already in module — **stdlib `flag`** preferred to avoid new deps. Required flags: **`--addr string`** (required for these subcommands), **`--insecure-skip-verify bool`** (default **false**, D-07). Map optional **`--timeout`** to **`context.WithTimeout`** for **`Dial`/`CreateSession`/`JoinSession`**. Print **`session_id`**, **`invite_code`**, **`peer_id`** to stdout for scripts.

Purpose: Human/script smoke (D-05); **canonical CLNT proof remains `go test`** (D-03).

Output: Buildable **`go build -o tunnel ./cmd/tunnel`** (or document module path).
</objective>

<verification>
- `go build -o /dev/null ./cmd/tunnel` succeeds (or `go build ./cmd/tunnel`).
- `grep -q insecure-skip-verify cmd/tunnel/client.go` exits 0.
- `grep -q -- '--addr' cmd/tunnel/client.go` exits 0.
</verification>

<tasks>

<task type="auto">
<name>Task: cmd/tunnel main and dispatch</name>
<files>cmd/tunnel/main.go</files>
<objective>Root command and client subcommand routing.</objective>
<read_first>
- cmd/tunnel/main.go (create)
- `/Users/ivan/repos/tunnel/pkg/client/client.go` — `Dial`, `CreateSession`, `JoinSession` signatures
- `/Users/ivan/repos/tunnel/.planning/phases/08-client-pkg-client-cmd/08-CONTEXT.md` (D-05, D-06)
</read_first>
<action>
Create **`cmd/tunnel/main.go`** with **`package main`**. Parse **`os.Args`**: usage **`tunnel client <create|join> [flags]`**. Delegate to **`runClientCreate`**, **`runClientJoin`** in **`cmd/tunnel/client.go`**. Exit code **1** on **`flag`** errors or client errors; print **`fmt.Fprintln(os.Stderr, err)`**. Do **not** add **`--insecure-skip-verify`** default true anywhere.
</action>
<acceptance_criteria>
- `grep -q 'package main' cmd/tunnel/main.go` exits 0.
- `go build -o /dev/null ./cmd/tunnel` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'package main' cmd/tunnel/main.go &amp;&amp; go build -o /dev/null ./cmd/tunnel</automated>
</verify>
<done>main.go parses tunnel client create|join and delegates.</done>
</task>

<task type="auto">
<name>Task: client.go flags (--addr, --insecure-skip-verify) + CreateSession/JoinSession</name>
<files>cmd/tunnel/client.go</files>
<objective>TLS flags and client create/join implementation.</objective>
<read_first>
- cmd/tunnel/client.go (create)
- pkg/client/client.go
- `crypto/tls` — `tls.Config{RootCAs, InsecureSkipVerify, ServerName}`
</read_first>
<action>
Create **`cmd/tunnel/client.go`**: **`package main`**. For **`client create`**: **`flag.String("addr", "", "host:port")`**, **`flag.Bool("insecure-skip-verify", false, "...")`**, optional **`timeout`**. **`tls.Config`**: **`RootCAs: nil`** (system) when **`!insecure`**; **`InsecureSkipVerify: true`** only when flag set (D-07). **`ServerName`**: hostname from **`addr`** if not IP literal (document in comment). Call **`client.Dial(ctx, addr, tlsCfg)`**, **`CreateSession`**, print **`session_id`** and **`invite_code`** newline-separated. For **`client join`**: add **`--session`** and **`--invite`** string flags (one required): if **`--session` set**, **`JoinSession(ctx, true, val)`**; else **`JoinSession(ctx, false, val)`**. Print **`peer_id`** in decimal. Per D-08, **no** client certificates.
</action>
<acceptance_criteria>
- `grep -q 'InsecureSkipVerify' cmd/tunnel/client.go` exits 0.
- `grep -q 'CreateSession' cmd/tunnel/client.go` exits 0.
- `grep -q 'JoinSession' cmd/tunnel/client.go` exits 0.
- `go build -o /dev/null ./cmd/tunnel` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'InsecureSkipVerify' cmd/tunnel/client.go &amp;&amp; grep -q 'CreateSession' cmd/tunnel/client.go &amp;&amp; grep -q 'JoinSession' cmd/tunnel/client.go &amp;&amp; go build -o /dev/null ./cmd/tunnel</automated>
</verify>
<done>client.go wires flags and pkg/client; binary builds.</done>
</task>

</tasks>

<success_criteria>
- CLI builds; **`--help`** or usage text mentions **`--addr`** and **`--insecure-skip-verify`** explicitly.
- No silent TLS verification bypass (grep for **`InsecureSkipVerify: true`** only behind flag parsing).
</success_criteria>

<output>
After completion, create `.planning/phases/08-client-pkg-client-cmd/08-03-SUMMARY.md`.
</output>
