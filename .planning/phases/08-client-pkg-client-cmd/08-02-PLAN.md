---
phase: 08-client-pkg-client-cmd
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - pkg/client/client.go
  - pkg/client/errors.go
  - pkg/client/doc.go
  - pkg/client/client_test.go
  - docs/client-stream-ids.md
autonomous: true
requirements:
  - CLNT-01
  - CLNT-02
  - CLNT-03
must_haves:
  truths:
    - "Client dials TCP+TLS, completes SESSION_CREATE, receives session_id and invite_code via protocol.DecodeSessionCreateAck."
    - "Client completes SESSION_JOIN and receives non-zero peer_id via protocol.DecodeSessionJoinAck."
    - "After JOIN_ACK, client sends and receives STREAM_DATA; broadcast (stream_id=1) and unicast (stream_id=2) paths are tested; pre-join SendStreamData is blocked (JoinGate)."
  artifacts:
    - path: pkg/client/client.go
      provides: "Dial, CreateSession, JoinSession, SendStreamData, read loop"
    - path: docs/client-stream-ids.md
      provides: "Documented fixed stream_id 1 (broadcast demo) and 2 (unicast demo) per D-11"
---

<objective>
Implement **`pkg/client`**: synchronous **`Client`** (D-09) over **`crypto/tls.Conn`** with **`context.Context`** for deadlines/cancel; **read buffer** + **`framing.ParseFrame`** loop; **`protocol.Encode*`** / **`Decode*`** for control and **`STREAM_DATA`**; enforce **`protocol.JoinGateAllowsBusinessDataPlane`** before sending **`MsgTypeStreamData`** (PROT-02 / D-10). Document **`stream_id`** **1** = broadcast demo, **2** = unicast demo in **`pkg/client/doc.go`** package comment **and** **`docs/client-stream-ids.md`** (D-11).

Purpose: Satisfy **CLNT-01, CLNT-02, CLNT-03** with **`go test ./pkg/client`** against **`internal/fakepeer`** (D-02, D-03).

Output: Production-oriented API stable for Phase 9–11; typed errors exposing **`framing.ErrCode`** where applicable (D-10).
</objective>

<verification>
- **`go test ./pkg/client -count=1`** exits 0.
- **`go test ./... -count=1`** exits 0.
- Grep doc strings: `grep -q 'stream_id' docs/client-stream-ids.md` and `grep -q 'stream_id' pkg/client/doc.go`.
</verification>

<tasks>

<task type="auto">
<name>Task: Client skeleton, Dial, framing loop, errors, doc.go stream_id note</name>
<files>pkg/client/client.go, pkg/client/errors.go, pkg/client/doc.go</files>
<objective>TLS dial helper and framing read/write primitives with error taxonomy.</objective>
<read_first>
- `/Users/ivan/repos/tunnel/pkg/client/client.go` (create)
- `/Users/ivan/repos/tunnel/pkg/client/errors.go` (create)
- `/Users/ivan/repos/tunnel/internal/fakepeer/harness.go`
- `/Users/ivan/repos/tunnel/pkg/framing/decode.go`
- `/Users/ivan/repos/tunnel/pkg/framing/errors.go`
</read_first>
<action>
Add **`pkg/client/errors.go`**: define **`ErrNotJoined`** (sentinel), **`ProtocolError` struct { Code framing.ErrCode; Reason string }`**, and transport/framing wrappers as needed (D-10); use **`errors.Is`/`As`**-friendly patterns. Add **`pkg/client/client.go`** with **`type Client struct`** holding **`conn *tls.Conn`**, **`readBuf []byte`**, **`joined bool`**, **`peerID uint64`**, **`capability uint32`** (default **0** for **`framing.AppendFrame`**). Implement **`func Dial(ctx context.Context, addr string, tlsCfg *tls.Config) (*Client, error)`** using **`(&net.Dialer{}).DialContext(ctx, "tcp", hostport)`** then **`tls.Client`**: if **`tlsCfg.ServerName==""`** and host is not IP, set **SNI** from hostname part of **addr**. Implement unexported **`func (c *Client) writeFrame(ctx context.Context, payload []byte) error`** setting deadline from **`ctx`** and **`framing.AppendFrame(framing.Frame{Version: framing.VersionV1, Capability: 0, Payload: payload})`**. Implement **`func (c *Client) readFrame(ctx context.Context) (framing.Frame, error)`** growing **`readBuf`**, looping **`ParseFrame`** until not **`ErrNeedMore`**; map **`ErrFrameTooLarge`/`ErrProtoVersion`** to typed errors. Package comment in **`pkg/client/doc.go`**: state **demo `stream_id`**: **1** = broadcast, **2** = unicast (D-11).
</action>
<acceptance_criteria>
- `grep -q 'type Client struct' pkg/client/client.go` exits 0.
- `grep -q 'Dial(ctx context.Context' pkg/client/client.go` exits 0.
- `go test ./pkg/client -run TestNonExistent -count=1` compiles.
</acceptance_criteria>
<verify>
  <automated>grep -q 'type Client struct' pkg/client/client.go &amp;&amp; grep -q 'Dial(ctx context.Context' pkg/client/client.go &amp;&amp; go test ./pkg/client -run TestNonExistent -count=1</automated>
</verify>
<done>Client, Dial, readFrame/writeFrame, errors, doc.go stream_id demo lines.</done>
</task>

<task type="auto">
<name>Task: CreateSession + JoinSession + TestClient_CreateSession / TestClient_JoinSession</name>
<files>pkg/client/client.go, pkg/client/client_test.go</files>
<objective>SESSION_CREATE and SESSION_JOIN APIs (CLNT-01, CLNT-02).</objective>
<read_first>
- pkg/client/client.go
- `/Users/ivan/repos/tunnel/pkg/protocol/control.go` — `EncodeSessionCreateReq`, `DecodeSessionCreateAck`, `EncodeSessionJoinReq`, `DecodeSessionJoinAck`
- `/Users/ivan/repos/tunnel/.planning/phases/08-client-pkg-client-cmd/08-CONTEXT.md` (D-09)
</read_first>
<action>
On **`Client`**, implement **`func (c *Client) CreateSession(ctx context.Context) (sessionID string, inviteCode string, err error)`**: send **`protocol.EncodeSessionCreateReq()`** via **`writeFrame`**; loop **`readFrame`** until **`payload[0]==protocol.MsgTypeSessionCreateAck`**; return **`protocol.DecodeSessionCreateAck`**. Implement **`func (c *Client) JoinSession(ctx context.Context, bySessionID bool, credential string) (peerID uint64, err error)`** with **`joinBy := uint8(0)`** if **`bySessionID`** else **`1`**, **`protocol.EncodeSessionJoinReq(joinBy, credential)`**; read until **`MsgTypeSessionJoinAck`**; **`protocol.DecodeSessionJoinAck`** → set **`c.peerID`**, **`c.joined=true`**. On **`MsgTypeProtocolError`**, **`protocol.DecodeProtocolError`** → return **`&ProtocolError{...}`**. Add **`pkg/client/client_test.go`**: **`TestClient_CreateSession`**, **`TestClient_JoinSession`** using **`internal/fakepeer`** **`Harness`** from 08-01 (**`tls.Config.RootCAs`** from cert helper**). Assert **`sessionID`/`invite`** match **`h.SessionID()`/`h.InviteCode()`** and **`peerID!=0`**.
</action>
<acceptance_criteria>
- `go test ./pkg/client -run TestClient_CreateSession -count=1` exits 0.
- `go test ./pkg/client -run TestClient_JoinSession -count=1` exits 0.
- `grep -q 'internal/fakepeer' pkg/client/client_test.go` exits 0.
</acceptance_criteria>
<verify>
  <automated>go test ./pkg/client -run TestClient_CreateSession -count=1 &amp;&amp; go test ./pkg/client -run TestClient_JoinSession -count=1 &amp;&amp; grep -q 'internal/fakepeer' pkg/client/client_test.go</automated>
</verify>
<done>CreateSession/JoinSession implemented; CLNT-01/02 tests pass against fakepeer.</done>
</task>

<task type="auto">
<name>Task: SendStreamData, ReadFrame/ReadStreamData, tests CLNT-03, docs/client-stream-ids.md</name>
<files>pkg/client/client.go, pkg/client/client_test.go, docs/client-stream-ids.md</files>
<objective>STREAM_DATA send/recv with JoinGate; broadcast + unicast tests (CLNT-03); docs file.</objective>
<read_first>
- pkg/client/client.go
- `/Users/ivan/repos/tunnel/pkg/protocol/streamdata.go` — `EncodeStreamData`, `DecodeStreamData`
- `/Users/ivan/repos/tunnel/pkg/protocol/routing.go` — `RoutingPrefix`, `RoutingModeBroadcast`, `RoutingModeUnicast`, `ValidateRoutingIntent`
- `/Users/ivan/repos/tunnel/pkg/protocol/join_gate.go`
- `/Users/ivan/repos/tunnel/docs/spec/v1/routing-modes.md`
</read_first>
<action>
Implement **`func (c *Client) SendStreamData(ctx context.Context, mode uint8, dstPeerID uint64, streamID uint32, flags uint8, app []byte) error`**: build **`protocol.RoutingPrefix`** with **`MsgType: protocol.MsgTypeStreamData`**, **`RoutingMode: mode`**, **`SrcPeerID: c.peerID`**, **`DstPeerID: dstPeerID`**; **`protocol.ValidateRoutingIntent(mode, dstPeerID)`**; **`payload, err := protocol.EncodeStreamData(prefix, streamID, flags, app)`** — note **`EncodeStreamData`** overwrites byte 0 with **`MsgTypeStreamData`**; call **`JoinGateAllowsBusinessDataPlane(c.joined, payload)`**; if not allowed return **`ErrNotJoined`**. **`writeFrame`**. Add **`func (c *Client) ReadFrame(ctx context.Context) (framing.Frame, error)`** or **`ReadStreamData(ctx)`** returning decoded **`protocol.StreamDataView`** for tests — must **`JoinGateAllowsBusinessDataPlane(c.joined, payload)`** on receive for **`STREAM_*`** or document that receive uses same gate (align with **`session-state.md`**). Add **`TestClient_StreamData`**: two **`Client`**s from **`Dial`** to same harness **addr**; A **CreateSession**, B **Join** with **`invite`**, A **Join** with **`session_id`**; A sends **broadcast** **`STREAM_DATA`** **`stream_id=1`**, **`RoutingModeBroadcast`**, app **`[]byte("bc")`** — B must receive same app on **`stream_id==1`**; B sends **unicast** to A’s **`peer_id`** with **`stream_id=2`** — only A receives. Add **`TestClient_SendStreamData_NotJoined`** expecting **`ErrNotJoined`** (no **`STREAM_DATA`** on wire). Create **`docs/client-stream-ids.md`** stating: demo **broadcast** uses **`stream_id=1`**, **unicast** uses **`stream_id=2`**, non-zero per **STREAM-02**; reference **`docs/spec/v1/streams-lifecycle.md`**.
</action>
<acceptance_criteria>
- `go test ./pkg/client -run TestClient_StreamData -count=1` exits 0.
- `go test ./pkg/client -run TestClient_SendStreamData_NotJoined -count=1` exits 0.
- `grep -q 'stream_id' docs/client-stream-ids.md` exits 0.
- `grep -q 'JoinGateAllowsBusinessDataPlane' pkg/client/client.go` exits 0.
</acceptance_criteria>
<verify>
  <automated>go test ./pkg/client -run TestClient_StreamData -count=1 &amp;&amp; go test ./pkg/client -run TestClient_SendStreamData_NotJoined -count=1 &amp;&amp; grep -q 'stream_id' docs/client-stream-ids.md &amp;&amp; grep -q 'JoinGateAllowsBusinessDataPlane' pkg/client/client.go</automated>
</verify>
<done>STREAM_DATA paths + docs; CLNT-03 + NotJoined tests pass.</done>
</task>

</tasks>

<success_criteria>
- **CLNT-01..03** covered by automated tests named above.
- **`go test ./... -count=1`** passes repository-wide.
</success_criteria>

<output>
After completion, create `.planning/phases/08-client-pkg-client-cmd/08-02-SUMMARY.md`.
</output>
