---
phase: 08-client-pkg-client-cmd
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/fakepeer/tlscert.go
  - internal/fakepeer/harness.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Tests can dial TLS to 127.0.0.1:0 and complete SESSION_CREATE_ACK with spec-valid session_id and invite_code."
    - "A second connection can SESSION_JOIN and receive SESSION_JOIN_ACK with non-zero peer_id."
    - "Fake routes STREAM_DATA broadcast (dst_peer_id=0) to other peers in the session and unicast by dst_peer_id."
  artifacts:
    - path: internal/fakepeer/harness.go
      provides: "TLS listener + per-conn framing loop + minimal session registry (package name not relay)."
---

<objective>
Implement **`internal/fakepeer`**: a **minimal TLS test harness** (not production Relay; **package name MUST NOT be `relay`**, per D-04) that speaks **v1 framing** (`framing.ParseFrame` / `framing.AppendFrame`) and **`pkg/protocol`** payloads for **SESSION_CREATE** / **SESSION_JOIN** / **STREAM_DATA** routing, so **`pkg/client`** integration tests can prove CLNT-01..03 without Docker (D-03).

Purpose: Isolate wire-compatible fake behavior in wave 1 so **`pkg/client`** (08-02) can import it and run **`go test`** as the canonical verifier (D-02).

Output: Runnable harness API (e.g. start/stop or test helper) + TLS cert material (dynamic **`tls.Certificate`** or **`testdata/*.pem`**).
</objective>

<verification>
- **`go test ./internal/fakepeer/... -count=1`** exits 0 after tasks complete.
- Grep: `grep -q 'package fakepeer' internal/fakepeer/harness.go` and `! grep -q '^package relay' internal/fakepeer/*.go`.
</verification>

<tasks>

<task type="auto">
<name>Task: Localhost TLS cert helper (fakepeer)</name>
<files>internal/fakepeer/tlscert.go</files>
<objective>Add TLS certificate helper for loopback tests.</objective>
<read_first>
- internal/fakepeer/tlscert.go (create)
- `crypto/tls`, `crypto/x509`, `encoding/pem` (stdlib patterns)
</read_first>
<action>
Create **`internal/fakepeer/tlscert.go`** with **`package fakepeer`**. Export a function **`LocalhostTLSConfig(t *testing.T) (*tls.Config, *x509.CertPool)`** (or **`TestServerTLSConfig`**) that returns a **`tls.Config`** suitable for **`tls.Server`**: server cert+key for **`127.0.0.1`** / **`localhost`** SAN, and a **`CertPool`** containing the same CA so **`tls.Client`** can verify without **`InsecureSkipVerify`**. Use **`t.Helper()`**; on failure call **`t.Fatal`**. Do **not** shell out to **openssl** (pure Go). This satisfies D-07 test path (explicit skip not required in tests when pool is wired).
</action>
<acceptance_criteria>
- `grep -q 'func.*TLS' internal/fakepeer/tlscert.go` exits 0.
- `go test ./internal/fakepeer -run TestNonExistent -count=1` compiles (package builds).
</acceptance_criteria>
<verify>
  <automated>grep -q 'func.*TLS' internal/fakepeer/tlscert.go &amp;&amp; go test ./internal/fakepeer -run TestNonExistent -count=1</automated>
</verify>
<done>tlscert.go exports TLS config + pool; package compiles.</done>
</task>

<task type="auto">
<name>Task: Harness control-plane (CREATE/JOIN)</name>
<files>internal/fakepeer/harness.go</files>
<objective>Implement TLS listener and framed control-plane handshake for one session.</objective>
<read_first>
- internal/fakepeer/harness.go (create)
- `/Users/ivan/repos/tunnel/pkg/framing/decode.go` — `ParseFrame`, `AppendFrame`, `Frame`, `ErrNeedMore`
- `/Users/ivan/repos/tunnel/pkg/protocol/control.go` — `DecodeSessionCreateReq`, `EncodeSessionCreateAck`, `DecodeSessionJoinReq`, `EncodeSessionJoinAck`
- `/Users/ivan/repos/tunnel/docs/spec/v1/session-create-join.md`
</read_first>
<action>
Create **`internal/fakepeer/harness.go`** with **`package fakepeer`**. Implement **`Harness`** struct holding: listener address, TLS config, mutable **`sessionID`** (UUID string matching **`protocol`** validation), **`inviteCode`** (8–12 base32 chars), **`nextPeerID uint64`** (start at **1**), **`map[uint64]*peerConn`** or equivalent for peers in session. Export **`NewHarness(t *testing.T) *Harness`** and **`func (h *Harness) Start(t *testing.T) (addr string, cleanup func())`**: **`net.ListenTCP("tcp", 127.0.0.1:0)`**, **`tls.NewListener`** with server TLS from tlscert helper. Per accepted **`net.Conn`**: read loop appending to **`[]byte`** buffer; repeatedly **`framing.ParseFrame`**; on full **`Frame`**, inspect **`f.Payload[0]`** with **`protocol.MsgType*`** constants:

1. **`MsgTypeSessionCreateReq`**: validate with **`protocol.DecodeSessionCreateReq`**, reply **`framing.AppendFrame(framing.Frame{Version: framing.VersionV1, Capability: 0, Payload: ack})`** where **`ack, err := protocol.EncodeSessionCreateAck(h.sessionID, h.inviteCode)`** (same strings every time for the harness lifetime).
2. **`MsgTypeSessionJoinReq`**: **`protocol.DecodeSessionJoinReq`** → if **`joinBy==0`**, credential must equal **`h.sessionID`**; if **`joinBy==1`**, credential must equal **`h.inviteCode`**. Then **`peerID := atomic increment`**, store conn, **`protocol.EncodeSessionJoinAck(peerID)`**, **`AppendFrame`** response.

Use **`context`**-friendly cleanup: **`cleanup`** closes listener and all tracked conns. Document in file comment that this is **test-only** and **not** Phase 9 Relay.
</action>
<acceptance_criteria>
- `grep -q 'EncodeSessionCreateAck' internal/fakepeer/harness.go` exits 0.
- `grep -q 'EncodeSessionJoinAck' internal/fakepeer/harness.go` exits 0.
- `grep -q 'ParseFrame' internal/fakepeer/harness.go` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'EncodeSessionCreateAck' internal/fakepeer/harness.go &amp;&amp; grep -q 'EncodeSessionJoinAck' internal/fakepeer/harness.go &amp;&amp; grep -q 'ParseFrame' internal/fakepeer/harness.go</automated>
</verify>
<done>harness.go implements Harness handshake with framing + protocol ACKs.</done>
</task>

<task type="auto">
<name>Task: Harness STREAM_DATA routing + TestHarness_CreateAndJoin</name>
<files>internal/fakepeer/harness.go, internal/fakepeer/harness_test.go</files>
<objective>Route STREAM_DATA broadcast and unicast between joined peers.</objective>
<read_first>
- internal/fakepeer/harness.go
- `/Users/ivan/repos/tunnel/pkg/protocol/streamdata.go` — `DecodeStreamData`, `EncodeStreamData`
- `/Users/ivan/repos/tunnel/pkg/protocol/routing.go` — `RoutingModeBroadcast`, `RoutingModeUnicast`, `ValidateRoutingIntent`
- `/Users/ivan/repos/tunnel/pkg/protocol/join_gate.go` — `JoinGateAllowsBusinessDataPlane`
</read_first>
<action>
Extend **`Harness`** so each peer connection tracks **`joined bool`** (true only after JOIN_ACK sent) and **`peerID uint64`**. On inbound **`Frame`** with **`payload[0]==protocol.MsgTypeStreamData`**: decode with **`protocol.DecodeStreamData`**. If **`!joined`**, respond with **`protocol.EncodeProtocolError`** (pick **`framing.ErrCode`** consistent with **`docs/spec/v1/errors.md`**, e.g. session state violation) inside **`AppendFrame`** OR drop per documented fake policy — **prefer** **`PROTOCOL_ERROR`** so client tests can assert decode path. If joined: for **`RoutingModeBroadcast`** (**`dst_peer_id==0`**), **`AppendFrame`** a copy to **every other** peer’s **`tls.Conn`** (not sender). For **`RoutingModeUnicast`**, deliver only to **`dst_peer_id`** if present in registry. Use **`protocol.EncodeStreamData`** when re-emitting (payload bytes may be forwarded as-is if lengths unchanged). Export **`SessionID()`** / **`InviteCode()`** getters for tests. Add **`internal/fakepeer/harness_test.go`** with **`TestHarness_CreateAndJoin`** dialing twice with **`tls.Client`**, running create on conn1, join both with **`session_id`** / second with **`invite`**, asserting **`peer_id`** 1 and 2 (or sequential non-zero). Run: **`go test ./internal/fakepeer -run TestHarness_CreateAndJoin -count=1`**.
</action>
<acceptance_criteria>
- `go test ./internal/fakepeer -run TestHarness_CreateAndJoin -count=1` exits 0.
- `grep -q 'DecodeStreamData' internal/fakepeer/harness.go` exits 0.
</acceptance_criteria>
<verify>
  <automated>go test ./internal/fakepeer -run TestHarness_CreateAndJoin -count=1 &amp;&amp; grep -q 'DecodeStreamData' internal/fakepeer/harness.go</automated>
</verify>
<done>STREAM_DATA forwarded; TestHarness_CreateAndJoin passes.</done>
</task>

</tasks>

<success_criteria>
- **`go test ./internal/fakepeer/... -count=1`** is green.
- No **`package relay`** under **`internal/fakepeer`** (`grep -R '^package relay' internal/fakepeer` returns nothing).
</success_criteria>

<output>
After completion, create `.planning/phases/08-client-pkg-client-cmd/08-01-SUMMARY.md` per execute-plan template.
</output>
