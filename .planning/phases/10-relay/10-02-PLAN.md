---
phase: 10-relay
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - pkg/relay/relay_test.go
autonomous: true
requirements:
  - RLY-03
must_haves:
  truths:
    - "Tests assert broadcast delivery to peer B when A sends; unicast from A to B; src_peer_id mismatch yields PROTOCOL_ERROR or client error."
  artifacts:
    - path: pkg/relay/relay_test.go
      provides: "Multi-client STREAM_DATA tests"
---

<objective>
为 **RLY-03** 添加 **`pkg/relay`** 集成测试：两 **`client.Client`** 同 **session**，**广播** 与 **单播** 各至少一条；**负例**：伪造错误 **`src_peer_id`**（需能构造 **`SendStreamData`** 或原始写帧 — 优先 **`SendStreamData`** 若可传入不一致 `SrcPeerID` — **若 Client 强制使用本地 peer_id`**，则通过 **`tls.Conn` + `framing.AppendFrame`** 发送畸形路由前缀并断言 **`PROTOCOL_ERROR`**）。

Purpose: 可重复自动化验证，与 Phase 11 E2E 区分（本 plan 仅 **relay 包内** 多连接）。
</objective>

<verification>
- `go test ./pkg/relay/... -count=1` exits 0.
- `go test ./... -count=1` exits 0.
</verification>

<tasks>

<task type="auto">
<name>Task: TestRelay_StreamData_Broadcast</name>
<files>pkg/relay/relay_test.go</files>
<objective>Two peers; A broadcasts; B receives one STREAM_DATA frame.</objective>
<read_first>
- `pkg/client/client.go` — `SendStreamData`, `ReadFrame`
- `pkg/protocol/routing.go` — `RoutingModeBroadcast`
- `docs/client-stream-ids.md` — stream_id 约定
</read_first>
<action>
Add **`TestRelay_StreamData_Broadcast`**：

- 启动 **`relay.Server`**（与现有测试相同 TLS 辅助）。
- **Conn A**：CreateSession → JoinSession(session) → **`SendStreamData(ctx, RoutingModeBroadcast, 0, streamID, 0, payload)`**（**`stream_id`** 使用 **1** 与 Phase 8 文档一致）。
- **Conn B**：JoinSession(session) → **`ReadFrame`** 循环直到 **`MsgTypeStreamData`**，断言 **payload** 或 **application** 字节与发送一致。

Use **`context.WithTimeout`**（如 **5s**）避免阻塞。
</action>
<acceptance_criteria>
- `go test ./pkg/relay -run TestRelay_StreamData_Broadcast -count=1` exits 0.
</acceptance_criteria>
<verify>
  <automated>go test ./pkg/relay -run TestRelay_StreamData_Broadcast -count=1</automated>
</verify>
<done>Broadcast path verified.</done>
</task>

<task type="auto">
<name>Task: TestRelay_StreamData_Unicast + bad src</name>
<files>pkg/relay/relay_test.go</files>
<objective>Unicast to peer 2; negative src_peer_id.</objective>
<read_first>
- `pkg/protocol/routing.go` — `RoutingModeUnicast`
- `pkg/relay/control.go` — src check
</read_first>
<action>
Add **`TestRelay_StreamData_Unicast`**：A 向 **`dst_peer_id = B.PeerID()`** 单播，B **`ReadFrame`** 收到数据。

Add **`TestRelay_StreamData_BadSrc`**：若 **`client.SendStreamData`** 始终用本地 **`peer_id` 作为 SrcPeerID`**，则对 **A** 使用 **`tls.Conn.Write`** 手工 **`AppendFrame`** 嵌入 **`SrcPeerID = 999`**（与 A 的真实 id 不符），读回 **`PROTOCOL_ERROR`** 并 **`DecodeProtocolError`** 期望 **`ErrCodeRoutingInvalid`**（或实现所选策略）。

若手工帧过繁，可改为在 **relay 测试** 中 **仅断言单播 + 广播** 两条，**bad src** 标为 **optional** — **本任务必须** 至少实现 **单播**；**bad src** 尽力而为，**若无法实现则 `t.Skip` 禁止** — **必须** 给出 **一种** 可运行负例（单播到不存在 **dst** 亦可）。
</action>
<acceptance_criteria>
- `go test ./pkg/relay -run 'TestRelay_StreamData_(Unicast|BadSrc)' -count=1` exits 0.
</acceptance_criteria>
<verify>
  <automated>go test ./pkg/relay -run 'TestRelay_StreamData_(Unicast|BadSrc)' -count=1</automated>
</verify>
<done>Unicast + negative coverage.</done>
</task>

</tasks>

<success_criteria>
- `go test ./pkg/relay/... -count=1` is green.
- `go test ./... -count=1` is green.
</success_criteria>

<output>
After completion, create `10-02-SUMMARY.md`.
</output>
