---
phase: 10-relay
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/relay/registry.go
  - pkg/relay/control.go
  - pkg/relay/conn.go
autonomous: true
requirements:
  - RLY-03
must_haves:
  truths:
    - "After JOIN_ACK, STREAM_DATA broadcast delivers to all session peers except sender; unicast delivers to dst_peer_id when present."
    - "Src_peer_id in frame matches connection peer or PROTOCOL_ERROR ERR_ROUTING_INVALID; ValidateRoutingIntent enforced."
  artifacts:
    - path: pkg/relay/registry.go
      provides: "JoinSession returns sessionID; DeliverStreamData fan-out"
    - path: pkg/relay/control.go
      provides: "DecodeStreamData + route branch"
---

<objective>
实现 **RLY-03** 核心：**`JOIN_ACK` 之后** 对 **`MsgTypeStreamData`** 解码（**`protocol.DecodeStreamData`**）、校验 **`SrcPeerID`** 与 **`ValidateRoutingIntent`**，并经由 **`SessionRegistry`** 向同 **session** 内其它 peer **写入完整帧**（**`framing.AppendFrame`**）。**广播**：`RoutingModeBroadcast`，**不**向 **`senderPeerID`** 回送。**单播**：`RoutingModeUnicast`，仅 **`dst_peer_id`** 对应 peer（若不存在则 **`PROTOCOL_ERROR`**，**`err_code`** 与 `errors.md` 一致，例如 **`ERR_ROUTING_INVALID`**）。**`STREAM_OPEN` / `STREAM_CLOSE`**：本 plan **不**实现转发 — 在已 JOIN 前提下返回 **`PROTOCOL_ERROR`**（UTF-8 reason 说明 phase 10 仅覆盖 **STREAM_DATA**），避免与成功标准混淆。

Purpose: 先落地数据面转发逻辑，**10-02** 专注测试与负例断言。
</objective>

<verification>
- `go test ./pkg/relay/... -count=1` exits 0（在 **10-02** 补全集成测试前，至少保证编译与既有测试仍通过；新测在 10-02）。
</verification>

<tasks>

<task type="auto">
<name>Task: JoinSession returns sessionID</name>
<files>pkg/relay/registry.go, pkg/relay/control.go</files>
<objective>Callers can address `session.peers` for data-plane routing.</objective>
<read_first>
- `pkg/relay/registry.go` — `JoinSession`, `session` struct
- `pkg/relay/control.go` — `MsgTypeSessionJoinReq` branch
</read_first>
<action>
Change **`JoinSession`** signature to **`(peerID uint64, sessionID string, err error)`** and return **`sess.id`** on success.

Update **`connState`** in **`pkg/relay/conn.go`** to add **`sessionID string`**. In **`control.go`** JOIN branch, after **`EncodeSessionJoinAck`**, set **`st.sessionID`** from return value.

Update **`pkg/relay/relay_test.go`** and any **`JoinSession(`** call sites to handle the new return signature（use **`_`** for **`sessionID`** if unused）。
</action>
<acceptance_criteria>
- `grep -q 'sessionID string' pkg/relay/registry.go` exits 0.
- `go build ./...` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'sessionID string' pkg/relay/registry.go &amp;&amp; go build ./...</automated>
</verify>
<done>JoinSession exposes session id to conn state.</done>
</task>

<task type="auto">
<name>Task: Registry DeliverStreamData</name>
<files>pkg/relay/registry.go</files>
<objective>Fan-out broadcast; unicast lookup; lock discipline.</objective>
<read_first>
- `pkg/protocol/routing.go` — `RoutingModeBroadcast`, `RoutingModeUnicast`
- `pkg/framing/decode.go` — `AppendFrame`
</read_first>
<action>
Add **`func (r *SessionRegistry) DeliverStreamData(sessionID string, senderPeerID uint64, mode uint8, dstPeerID uint64, payload []byte) error`**:

- Build **`wire := framing.AppendFrame(framing.Frame{Version: framing.VersionV1, Capability: 0, Payload: payload})`** once.
- Under **`r.mu`**, resolve **`sess := r.byID[sessionID]`**; if nil return **`ErrSessionNotFound`**.
- **Broadcast** (`mode == RoutingModeBroadcast`): for each **`peerID != senderPeerID`** in **`sess.peers`**, collect **`*Peer`**.
- **Unicast**: if **`sess.peers[dstPeerID] == nil`**, unlock and return **`errors.New("relay: dst peer not in session")`**（调用方映射为 **`EncodeProtocolError(ErrCodeRoutingInvalid, ...)`**）。
- **Release lock** before iterating **`Write(wire)`** on each **`FrameWriter`**.

Document in **godoc** that **sender does not receive** a copy on broadcast.
</action>
<acceptance_criteria>
- `grep -q 'DeliverStreamData' pkg/relay/registry.go` exits 0.
- `go test ./pkg/relay -run TestRelay_ClientCreateJoin -count=1` exits 0（回归 Phase 9 集成测试）.
</acceptance_criteria>
<verify>
  <automated>grep -q 'DeliverStreamData' pkg/relay/registry.go &amp;&amp; go test ./pkg/relay -run TestRelay_ClientCreateJoin -count=1</automated>
</verify>
<done>Registry can deliver stream frames to peers.</done>
</task>

<task type="auto">
<name>Task: control.go STREAM_DATA routing</name>
<files>pkg/relay/control.go</files>
<objective>Replace phase-9 placeholder with decode + DeliverStreamData.</objective>
<read_first>
- `pkg/protocol/streamdata.go` — `DecodeStreamData`
- `pkg/protocol/join_gate.go`
- `pkg/relay/registry.go` — `DeliverStreamData`
</read_first>
<action>
Split handling: **`MsgTypeStreamData`** alone uses full routing path.

For **`STREAM_DATA`** after **`JoinGateAllowsBusinessDataPlane`** passes:

1. **`v, err := protocol.DecodeStreamData(f.Payload)`** — on error **`EncodeProtocolError(ErrCodeRoutingInvalid, ...)`**.
2. If **`v.Prefix.SrcPeerID != st.peerID`** → **`PROTOCOL_ERROR`** **`ERR_ROUTING_INVALID`**.
3. **`ValidateRoutingIntent(v.Prefix.RoutingMode, v.Prefix.DstPeerID)`** — on error same.
4. **`reg.DeliverStreamData(st.sessionID, st.peerID, v.Prefix.RoutingMode, v.Prefix.DstPeerID, f.Payload)`** — map **`ErrSessionNotFound`** / dst missing to appropriate **`ErrCode`**（与 **`docs/spec/v1/errors.md`** 一致）.
5. On success **`return nil`**（**不**向发送者写回业务 ACK）。

**`STREAM_OPEN` / `STREAM_CLOSE`**：单独 **`case`**（或保留合并但 **`switch mt`** 内先分支）：JOIN 后仍返回 **`EncodeProtocolError`**，reason 含 **`STREAM_OPEN/STREAM_CLOSE not implemented`**。

Ensure **`st.sessionID != ""`** before deliver —否则 **`PROTOCOL_ERROR`**。
</action>
<acceptance_criteria>
- `grep -q 'DecodeStreamData' pkg/relay/control.go` exits 0.
- `grep -q 'DeliverStreamData' pkg/relay/control.go` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'DecodeStreamData' pkg/relay/control.go &amp;&amp; grep -q 'DeliverStreamData' pkg/relay/control.go</automated>
</verify>
<done>STREAM_DATA forwarded per mode.</done>
</task>

</tasks>

<success_criteria>
- `go build ./...` succeeds.
- Existing **`TestRelay_ClientCreateJoin`** still passes.
</success_criteria>

<output>
After completion, create `10-01-SUMMARY.md`.
</output>
