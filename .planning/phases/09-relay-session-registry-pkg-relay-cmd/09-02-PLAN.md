---
phase: 09-relay-session-registry-pkg-relay-cmd
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - pkg/relay/registry.go
  - pkg/relay/control.go
  - pkg/relay/conn.go
  - pkg/relay/server.go
  - pkg/relay/relay_test.go
autonomous: true
requirements:
  - RLY-02
must_haves:
  truths:
    - "Process-local Session Registry maps session_id ↔ peers; SESSION_CREATE returns spec-valid session_id and invite_code; SESSION_JOIN returns non-zero peer_id."
    - "Each joined peer is registered with a writable connection for later RLY-03 routing (store *tls.Conn or equivalent)."
  artifacts:
    - path: pkg/relay/registry.go
      provides: "SessionRegistry with CreateSession, JoinSession"
    - path: pkg/relay/control.go
      provides: "Dispatch MsgTypeSessionCreateReq / MsgTypeSessionJoinReq"
---

<objective>
Deliver **RLY-02**：在 **`09-01`** 的 **`Server` + 成帧循环** 上实现进程内 **`SessionRegistry`**：处理 **`SESSION_CREATE_REQ`**（生成 **UUID `session_id`**、**8–12** 字符 **invite_code** 符合 **`protocol.validInviteCode`**）、**`SESSION_JOIN_REQ`**（**`join_by`** 0 = session_id / 1 = invite），分配 **`peer_id`**（从 **1** 递增），并发送 **`EncodeSessionCreateAck` / `EncodeSessionJoinAck`**。对 **`MsgTypeStreamData`**：**JOIN_ACK 之前** **MUST** 拒绝 — 使用 **`protocol.JoinGateAllowsBusinessDataPlane(false, ...)`** 判定并 **`EncodeProtocolError`**（**`ERR_ROUTING_INVALID`** 或与 `errors.md` 一致的码）。**不**实现广播/单播转发（Phase 10）。

Purpose: 与 **`pkg/client`** 互通验证 **CLNT-01/02** 对真实 Relay；为 **Phase 10** 留下 peer 表与 **`Write`** 目标。
</objective>

<verification>
- **`go test ./pkg/relay/... -count=1`** exits 0.
- **`go test ./pkg/client/... -count=1`** still exits 0 (no regressions).
</verification>

<tasks>

<task type="auto">
<name>Task: SessionRegistry data structures</name>
<files>pkg/relay/registry.go</files>
<objective>In-memory registry: sessions, peers, peer_id allocation.</objective>
<read_first>
- `/Users/ivan/repos/tunnel/docs/spec/v1/session-create-join.md` (if present) or `pkg/protocol/control.go` comments
- `/Users/ivan/repos/tunnel/pkg/protocol/control.go` — validation rules for session_id and invite
</read_first>
<action>
Create **`pkg/relay/registry.go`**:
- **`type SessionRegistry struct`** with **`sync.Mutex`**, maps: **`sessions map[string]*session`** where **`session`** holds **`sessionID string`**, **`inviteCode string`**, **`nextPeerID uint64`** (start **0**, first assigned **1**), **`peers map[uint64]*Peer`**.
- **`type Peer struct`** with **`ID uint64`**, **`Write func([]byte) (int, error)`** or **`*tls.Conn`** — choose **`interface{ Write([]byte)(int,error) }`** named **`FrameWriter`** for testability.
- **`func (r *SessionRegistry) CreateSession() (sessionID, inviteCode string, err error)`**: generate UUID v4 string (**`github.com/google/uuid`** add to **go.mod** if not present), **`inviteCode`** random base32 **8–12** chars using alphabet from **`protocol`** (duplicate charset constant or use **`crypto/rand`** + validate with **`protocol`** by round-tripping **`EncodeSessionCreateAck`**).
- **`func (r *SessionRegistry) JoinSession(joinBy uint8, credential string) (sessionID string, peerID uint64, err error)`**: lookup session by **`session_id`** or **`invite`**; on success allocate **`peerID := atomic increment`**, register placeholder peer (writer wired later in **`RegisterPeer`**).
- Export **`var ErrSessionNotFound`** / **`ErrJoinDenied`** as **`errors.Is`**-friendly for control layer.

Keep **all** exported identifiers **documented** in **godoc** one-liners.
</action>
<acceptance_criteria>
- `grep -q 'type SessionRegistry' pkg/relay/registry.go` exits 0.
- `go test ./pkg/relay -run TestNonExistent -count=1` compiles.
</acceptance_criteria>
<verify>
  <automated>grep -q 'type SessionRegistry' pkg/relay/registry.go &amp;&amp; go test ./pkg/relay -run TestNonExistent -count=1</automated>
</verify>
<done>Registry compiles with Create/Join skeleton.</done>
</task>

<task type="auto">
<name>Task: Wire registry to Server + per-conn control dispatch</name>
<files>pkg/relay/server.go, pkg/relay/control.go, pkg/relay/conn.go</files>
<objective>Attach `*SessionRegistry` to `Server`; handle CREATE/JOIN in serveConn.</objective>
<read_first>
- `pkg/relay/registry.go`
- `/Users/ivan/repos/tunnel/pkg/protocol/msgtype.go`
- `/Users/ivan/repos/tunnel/pkg/protocol/control.go`
- `/Users/ivan/repos/tunnel/pkg/protocol/join_gate.go`
</read_first>
<action>
Add **`pkg/relay/control.go`** with **`func (s *Server) handleControl(conn *tls.Conn, f framing.Frame, reg *sessionConnState) error`** where **`sessionConnState`** tracks **`joined bool`**, **`peerID uint64`**, **`sessionID string`**.

- **`Server`** gains **`Registry *SessionRegistry`** field (set in tests / **`NewServer`**).

- In **`serveConn`**, after successful **`ParseFrame`**, switch **`f.Payload[0]`**:
  - **`MsgTypeSessionCreateReq`**: **`DecodeSessionCreateReq`**, **`reg.CreateSession()`** via registry, **`EncodeSessionCreateAck`**, **`Write(AppendFrame(...))`**. Mark **`created`** for this connection as creator peer — **first peer in session**: allocate **`peer_id=1`** immediately **or** follow spec: **CREATE_ACK** does not assign peer — **check `session-create-join.md`**: typically **creator** may need **JOIN** too; **minimal behavior**: after **CREATE**, same connection is **implicit peer 1** **or** require **JOIN** — **use same semantics as `internal/fakepeer`**: harness assigns peer on **JOIN** only; **CREATE** only returns ids. Then **creator** must **JOIN** with **session_id**. **Align with fakepeer**: **CREATE** then **JOIN** second step for same conn **or** two conns — **simplest for RLY-02**: **CREATE** adds session; **first JOIN** on **any** connection with **session_id** gets **peer_id=1**. Document in code.

- **`MsgTypeSessionJoinReq`**: **`DecodeSessionJoinReq`**, **`JoinSession`**, **`EncodeSessionJoinAck`**, set **`joined=true`**, **`peerID`**, register **`Peer`** with **`conn.Write`** shim for **`AppendFrame`** writes.

- **`MsgTypeStreamData`**: if **`!joined`** OR **`!JoinGateAllowsBusinessDataPlane(joined, ...)`** → **`EncodeProtocolError`** per PROT-02.

Remove placeholder **`"relay: control plane not ready"`** from **`09-01`** when paths are live.

Use **`sync.Mutex`** for registry mutations; avoid holding lock while **`Write`** to TLS (release lock before write if needed).
</action>
<acceptance_criteria>
- `grep -q 'MsgTypeSessionCreateReq' pkg/relay/control.go` exits 0.
- `grep -q 'MsgTypeSessionJoinReq' pkg/relay/control.go` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'MsgTypeSessionCreateReq' pkg/relay/control.go &amp;&amp; grep -q 'MsgTypeSessionJoinReq' pkg/relay/control.go</automated>
</verify>
<done>CREATE/JOIN ACK paths implemented.</done>
</task>

<task type="auto">
<name>Task: Integration test — pkg/client against pkg/relay</name>
<files>pkg/relay/relay_test.go</files>
<objective>End-to-end: Dial relay, CreateSession, JoinSession, assert peer_ids.</objective>
<read_first>
- `/Users/ivan/repos/tunnel/pkg/client/client.go` — `Dial`, `CreateSession`, `JoinSession`
- `internal/fakepeer/tlscert.go`
- `pkg/relay/server.go`
</read_first>
<action>
Add **`pkg/relay/relay_test.go`** **`package relay_test`**:
- Start **`relay.Server`** on **`127.0.0.1:0`** with **`fakepeer.LocalhostTLSConfig`** server side; client **`tls.Config`** from same helper.

- **`client.Dial`**, **`CreateSession`**, assert **`session_id`** non-empty.

- Second connection: **`JoinSession`** with **`session_id`**, assert **`peer_id==2`** or sequential **1,2** depending on creator JOIN rule — **assert non-zero** and **distinct**.

- Optional: **`STREAM_DATA`** before JOIN → expect error / **`PROTOCOL_ERROR`** decode on client — if **`client`** does not expose, skip **or** raw **`ParseFrame`** read.

Run: **`go test ./pkg/relay -run TestRelay_ClientCreateJoin -count=1`**.
</action>
<acceptance_criteria>
- `go test ./pkg/relay -run TestRelay_ClientCreateJoin -count=1` exits 0.
</acceptance_criteria>
<verify>
  <automated>go test ./pkg/relay -run TestRelay_ClientCreateJoin -count=1</automated>
</verify>
<done>RLY-02 verified against real client.</done>
</task>

</tasks>

<success_criteria>
- **`go test ./pkg/relay/... -count=1`** is green.
- **`go test ./... -count=1`** is green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-relay-session-registry-pkg-relay-cmd/09-02-SUMMARY.md` per execute-plan template.
</output>
