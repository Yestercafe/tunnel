---
phase: 09-relay-session-registry-pkg-relay-cmd
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/relay/doc.go
  - pkg/relay/server.go
  - pkg/relay/conn.go
  - pkg/relay/server_test.go
  - cmd/tunnel/main.go
  - cmd/tunnel/relay.go
autonomous: true
requirements:
  - RLY-01
must_haves:
  truths:
    - "Relay listens on TCP and terminates TLS; each connection runs a read buffer + framing.ParseFrame loop consistent with pkg/framing."
    - "ErrNeedMore is handled by retaining buffer and reading more bytes; ErrFrameTooLarge and ErrProtoVersion yield PROTOCOL_ERROR frames per protocol.EncodeProtocolError."
  artifacts:
    - path: pkg/relay/server.go
      provides: "TLS listener, Serve, per-connection handler entry"
    - path: pkg/relay/conn.go
      provides: "Read loop with ParseFrame / AppendFrame write path"
---

<objective>
Deliver **RLY-01**：**`pkg/relay`** 提供 **`Server`**（TCP **`Listen`** + **`tls.NewListener`**），每条入站 TLS 连接在独立 goroutine 中维护 **读缓冲** 与 **`framing.ParseFrame`** / **`ErrNeedMore`** 循环；出站使用 **`framing.AppendFrame`**。在控制面 registry 尚未接线的本 plan 内，对 **完整解析后的帧** 可先做 **最小分发**：仅处理 **`ErrProtoVersion`** / **`ErrFrameTooLarge`** 等成帧层错误并回复 **`protocol.EncodeProtocolError`**；对合法 **`msg_type`** 可 **临时** 回复 **`PROTOCOL_ERROR`**（例如 `framing.ErrCode` 与 `errors.md` 一致的 session/state 占位）或 **静默丢弃** — **须在代码注释标明「09-02 替换为 CREATE/JOIN」**。**`cmd/tunnel relay`** 子命令：监听地址 + TLS **cert/key PEM 路径**（与 `tunnel client` 风格一致），便于手工冒烟。

Purpose: 把 **承载与成帧** 与 **Session Registry（09-02）** 解耦，便于 wave 并行度与测试隔离。
</objective>

<verification>
- **`go test ./pkg/relay/... -count=1`** exits 0.
- **`grep -q 'ParseFrame' pkg/relay/conn.go`** exits 0.
</verification>

<tasks>

<task type="auto">
<name>Task: pkg/relay package skeleton + Server TLS listen</name>
<files>pkg/relay/doc.go, pkg/relay/server.go</files>
<objective>Create `pkg/relay` with exported `Server` and `ListenAndServe` (or `Serve` + `Listen`) using TLS.</objective>
<read_first>
- `/Users/ivan/repos/tunnel/pkg/framing/decode.go` — `ParseFrame`, `ErrNeedMore`, `AppendFrame`, `Frame`, errors
- `/Users/ivan/repos/tunnel/pkg/protocol/control.go` — `EncodeProtocolError`, `MsgType` constants
- `/Users/ivan/repos/tunnel/docs/spec/v1/errors.md` — `ErrCode` semantics
- `/Users/ivan/repos/tunnel/internal/fakepeer/tlscert.go` — loopback TLS for tests (optional pattern)
</read_first>
<action>
Add **`pkg/relay/doc.go`** with **`package relay`** one-paragraph doc: real Relay (not fakepeer), Phase 9 RLY-01/02 scope boundary.

Add **`pkg/relay/server.go`**:
- **`type Server struct`** holding **`TLSConfig *tls.Config`**, **`Addr string`**, and a **`net.Listener`** field set after listen.
- **`func (s *Server) Listen() error`**: `net.Listen("tcp", s.Addr)` then `tls.NewListener(raw, s.TLSConfig)`; store inner listener; return on error.
- **`func (s *Server) Serve(ctx context.Context) error`**: accept loop; on **`Accept`**, spawn goroutine calling **`s.serveConn(conn)`** (stub in **`conn.go`**).
- **`func (s *Server) Close() error`**: close listener.

Use **`context`** for **`Serve`** shutdown: on **`ctx.Done()`**, close listener and return **`ctx.Err()`** if appropriate.

Do **not** implement SESSION_CREATE/JOIN in this task — only accept TLS and hand off to **`serveConn`**.
</action>
<acceptance_criteria>
- `grep -q 'package relay' pkg/relay/server.go` exits 0.
- `go test ./pkg/relay -run TestNonExistent -count=1` compiles (empty test file allowed in next task).
</acceptance_criteria>
<verify>
  <automated>grep -q 'package relay' pkg/relay/server.go &amp;&amp; go test ./pkg/relay -run TestNonExistent -count=1</automated>
</verify>
<done>Server listens with TLS; compiles.</done>
</task>

<task type="auto">
<name>Task: Per-connection read buffer + ParseFrame loop + PROTOCOL_ERROR on frame errors</name>
<files>pkg/relay/conn.go, pkg/relay/server.go</files>
<objective>Implement `serveConn` read loop: buffer, ParseFrame, handle framing-level errors.</objective>
<read_first>
- `pkg/relay/server.go`
- `/Users/ivan/repos/tunnel/pkg/framing/decode.go`
- `/Users/ivan/repos/tunnel/pkg/framing/errors.go` — `ErrCode` values
- `/Users/ivan/repos/tunnel/pkg/protocol/control.go` — `EncodeProtocolError`
</read_first>
<action>
Add **`pkg/relay/conn.go`** with **`func (s *Server) serveConn(conn *tls.Conn)`**:
- **`var buf []byte`** (or read into fixed **`make([]byte, 4096)`** and append to **`buf`**).
- Loop: **`read`** more data on **`ErrNeedMore`** from **`ParseFrame`**; on success **`n`**, **`buf = buf[n:]`** compact or slice.
- On **`framing.ErrFrameTooLarge`**: build payload with **`protocol.EncodeProtocolError(framing.ErrCodeFrameTooLarge, "ERR_FRAME_TOO_LARGE")`** (reason non-empty UTF-8), **`conn.Write(framing.AppendFrame(framing.Frame{Version: framing.VersionV1, Capability: 0, Payload: p}))`**, then **`return`** (close conn in defer from **`Serve`** or explicit **`conn.Close()`**).
- On **`framing.ErrProtoVersion`**: **`EncodeProtocolError`** with **`ErrCodeProtoVersion`**, write frame, return.
- On **`ParseFrame` success** with a **`Frame`**: if **`len(f.Payload)==0`**, close/return; else inspect **`f.Payload[0]`** — for this plan only, **may** **`continue`** reading next frames **without** full registry **or** send a single **`PROTOCOL_ERROR`** with a **documented placeholder** reason string **`"relay: control plane not ready (phase 09-02)"`** and stop — **pick one strategy and document in `conn.go` comment** so 09-02 can replace.

Ensure **no** **`package relay`** imports **`internal/fakepeer`** (avoid test coupling in library).
</action>
<acceptance_criteria>
- `grep -q 'ParseFrame' pkg/relay/conn.go` exits 0.
- `grep -q 'EncodeProtocolError' pkg/relay/conn.go` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'ParseFrame' pkg/relay/conn.go &amp;&amp; grep -q 'EncodeProtocolError' pkg/relay/conn.go</automated>
</verify>
<done>Framing loop + proto error responses for decode-level failures.</done>
</task>

<task type="auto">
<name>Task: Tests — TLS localhost + invalid version frame</name>
<files>pkg/relay/server_test.go</files>
<objective>Prove framing loop with tls.Client to Server.</objective>
<read_first>
- `pkg/relay/server.go`, `pkg/relay/conn.go`
- `/Users/ivan/repos/tunnel/internal/fakepeer/tlscert.go` — `LocalhostTLSConfig` or equivalent
- `/Users/ivan/repos/tunnel/pkg/framing/decode.go` — `AppendFrame` to craft bad version
</read_first>
<action>
Add **`pkg/relay/server_test.go`**:
- Helper **`startTestServer(t *testing.T) (addr string, stop func())`** using **`fakepeer.LocalhostTLSConfig`** (or duplicate minimal cert helper **inside `pkg/relay` test** if import path disallows — **prefer** `internal/fakepeer` from **`_test`** package: **`relay_test`** cannot import `internal/fakepeer` from `pkg/relay` — **actually** `go test` in **`pkg/relay`** **can** import **`internal/fakepeer`** because test files are **`package relay_test`** in **`pkg/relay_test`** — use **`package relay_test`** in **`server_test.go`** and import **`tunnel/internal/fakepeer`** + **`tunnel/pkg/relay`**).

- Test **`TestServer_ProtoVersionError`**: start server, **`tls.Dial`** with client config from fakepeer, send **`AppendFrame(Frame{Version: 0x9999, Capability: 0, Payload: []byte{0x01}})`** (invalid version), read response, **`protocol.DecodeProtocolError`** on returned payload after **`ParseFrame`** — expect **`ErrCodeProtoVersion`** or connection close per implementation.

- Test **`TestServer_NeedMore`**: send one byte at a time until full frame parses (optional, if timeboxed — else skip with `t.Skip` **not allowed**; must implement minimal version).

Run: **`go test ./pkg/relay -count=1 -v`** (or without `-v` in CI).
</action>
<acceptance_criteria>
- `go test ./pkg/relay -run TestServer_ProtoVersionError -count=1` exits 0.
</acceptance_criteria>
<verify>
  <automated>go test ./pkg/relay -run TestServer_ProtoVersionError -count=1</automated>
</verify>
<done>Automated proof of TLS + framing error path.</done>
</task>

<task type="auto">
<name>Task: cmd/tunnel relay subcommand</name>
<files>cmd/tunnel/main.go, cmd/tunnel/relay.go</files>
<objective>Expose relay server via CLI: --listen, --cert, --key.</objective>
<read_first>
- `/Users/ivan/repos/tunnel/cmd/tunnel/main.go`
- `/Users/ivan/repos/tunnel/cmd/tunnel/client.go` — flag patterns
- `pkg/relay/server.go`
</read_first>
<action>
- Update **`cmd/tunnel/main.go`**: add **`case "relay":`** calling **`runRelay(os.Args[2:])`**; update **`usage()`** to document **`tunnel relay --listen addr --cert path --key path`**.
- Add **`cmd/tunnel/relay.go`**: **`runRelay`** parses **`--listen`** (required), **`--cert`**, **`--key`** (both required for TLS); load **`tls.LoadX509KeyPair(cert, key)`**; build **`&tls.Config{Certificates: []tls.Certificate{cert}}`**; **`&relay.Server{Addr: listen, TLSConfig: tlsCfg}`**; **`Listen`**, then **`Serve(context.Background())`** with signal handling **optional** (may block until error).

Print listening address to **stdout** on success (one line).

Do **not** require **`--insecure`** on server side.
</action>
<acceptance_criteria>
- `grep -q 'runRelay' cmd/tunnel/main.go` exits 0.
- `grep -q 'relay' cmd/tunnel/main.go` exits 0.
- `go build -o /dev/null ./cmd/tunnel` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'runRelay' cmd/tunnel/main.go &amp;&amp; go build -o /dev/null ./cmd/tunnel</automated>
</verify>
<done>CLI can start relay listener with PEM certs.</done>
</task>

</tasks>

<success_criteria>
- **`go test ./pkg/relay/... -count=1`** is green.
- **`go build ./cmd/tunnel`** succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/09-relay-session-registry-pkg-relay-cmd/09-01-SUMMARY.md` per execute-plan template.
</output>
