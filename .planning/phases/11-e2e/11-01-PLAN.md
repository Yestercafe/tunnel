---
phase: 11-e2e
plan: "01"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/phases/11-e2e/11-VALIDATION.md
  - pkg/relay/relay_test.go
autonomous: true
requirements:
  - E2E-01
  - E2E-02
must_haves:
  truths:
    - "REQUIREMENTS traceability lists E2E-01 and E2E-02 as Complete with Phase 11."
    - "PROJECT.md reflects E2E demo / Phase 11 closure."
    - "relay_test.go documents REQ→test mapping including broadcast, unicast, illegal dst, and TestRelay_StreamData_BeforeJoinAck."
    - "11-VALIDATION.md lists concrete -run filters and ties rows to requirement IDs."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      contains: "| E2E-01"
    - path: pkg/relay/relay_test.go
      provides: "REQ ID comments adjacent to TestRelay_* E2E tests"
  key_links:
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: "pkg/relay/relay_test.go test function names"
      via: "Status Complete + comment anchors"
---

<objective>
将 **Phase 11** 的**需求追溯**与**验收文档**对齐到仓库内**已运行**的 `go test` 路径：显式声明 **E2E-01**（双 `client.Client`、`relay.Server`、同 session、广播/单播）由 `TestRelay_StreamData_Broadcast` / `TestRelay_StreamData_Unicast` 覆盖；**E2E-02** 由 `TestRelay_StreamData_UnicastMissingDst`（非法路由）与 **`TestRelay_StreamData_BeforeJoinAck`**（JOIN 前数据面，依赖 **11-02**）共同覆盖。更新 **PROJECT.md** 当前状态与 **ROADMAP** Phase 11 计划清单；刷新 **11-VALIDATION.md** 中占位符与 Nyquist 元数据。

Purpose: 满足「文档/可追溯性」交付，避免仅代码变更而无需求表/验证表同步。
Output: 更新的 `.planning/*.md` + `relay_test.go` 内联 REQ 注释（**不**改变测试逻辑）。
</objective>

<execution_context>
@/Users/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-e2e/11-VALIDATION.md
@.planning/phases/11-e2e/11-RESEARCH.md
@pkg/relay/relay_test.go
</context>

<tasks>

<task type="auto">
<name>Task: REQUIREMENTS traceability + PROJECT current state</name>
<files>.planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
<read_first>
- `.planning/REQUIREMENTS.md` — Traceability table rows for E2E-01, E2E-02
- `.planning/PROJECT.md` — Active / Validated sections
</read_first>
<action>
1. In `.planning/REQUIREMENTS.md` **Traceability** table: set **Status** for **E2E-01** and **E2E-02** to **Complete**（或项目一贯使用的完成标记，如 `Verified` — 与表中其它 Complete 行一致）；**Phase** 列保持 **11**。

2. Optionally add a **Coverage note** bullet under Traceability（单行即可）：列出  
   `TestRelay_StreamData_Broadcast` → E2E-01；`TestRelay_StreamData_Unicast` → E2E-01；`TestRelay_StreamData_UnicastMissingDst` → E2E-02；`TestRelay_StreamData_BeforeJoinAck` → E2E-02。

3. In `.planning/PROJECT.md` **Active（v1.1 实现）**：将 **E2E-DEMO** checkbox 标为已完成（`[x]`），并加短注「Phase 11：`pkg/relay/relay_test.go`」。

4. Update **Last updated** 行日期为执行日（2026 年）。
</action>
<acceptance_criteria>
- `grep -E 'E2E-01.*Complete|E2E-02.*Complete' .planning/REQUIREMENTS.md` 匹配成功（或 `grep 'E2E-01' .planning/REQUIREMENTS.md` 显示 Status 非 Pending）。
- `grep -q 'E2E-DEMO' .planning/PROJECT.md` && `grep '\[x\].*E2E-DEMO\|E2E-DEMO.*\[x\]' .planning/PROJECT.md` 退出 0（或肉眼确认该项已勾选）。
- `go test ./pkg/relay/... -run 'TestRelay_StreamData_(Broadcast|Unicast|UnicastMissingDst)' -count=1` 退出 0（E2E 证据链快检；`BeforeJoinAck` 在 11-02 合并后再纳入全量 `-run`）。
</acceptance_criteria>
<verify>
  <automated>grep 'E2E-01' .planning/REQUIREMENTS.md | grep -q Complete && grep 'E2E-02' .planning/REQUIREMENTS.md | grep -q Complete && grep -q 'E2E-DEMO' .planning/PROJECT.md && go test ./pkg/relay/... -run 'TestRelay_StreamData_(Broadcast|Unicast|UnicastMissingDst)' -count=1</automated>
</verify>
<done>需求表与 PROJECT 状态反映 Phase 11 完成。</done>
</task>

<task type="auto">
<name>Task: ROADMAP Phase 11 plans + relay_test REQ comments</name>
<files>.planning/ROADMAP.md, pkg/relay/relay_test.go</files>
<read_first>
- `.planning/ROADMAP.md` — Phase 11 block（Plans: TBD）
- `pkg/relay/relay_test.go` — existing `TestRelay_StreamData_*` functions
</read_first>
<action>
1. **ROADMAP.md** — 在 **Phase 11** 段：将 `**Plans:** TBD` 改为 `**Plans:** 2 plans`；用可勾选列表替换占位：
   - `11-01-PLAN.md` — 追溯与文档（E2E-01、E2E-02）
   - `11-02-PLAN.md` — `TestRelay_StreamData_BeforeJoinAck` + `UnderlyingTLSConn`（E2E-02）

   更新 **Progress** 表中 Phase 11 的 Plans Complete / Status 为合理占位（例如 `0/2` Not started → 执行完成后由执行器改为 `2/2` Complete；若本 task 仅更新模板，保持 `0/2` 直至执行完毕 — **推荐**：将文案改为 `2 plans defined`，Status **In progress** 直到 verify-work）。

2. **`pkg/relay/relay_test.go`** — 在下列测试函数**上方**添加单行注释（若已存在类似注释则合并，不重复）：
   - `TestRelay_StreamData_Broadcast` → `// E2E-01: two clients, same session, broadcast`
   - `TestRelay_StreamData_Unicast` → `// E2E-01: two clients, same session, unicast`
   - `TestRelay_StreamData_UnicastMissingDst` → `// E2E-02: illegal unicast dst → PROTOCOL_ERROR (ErrCodeRoutingInvalid)`
   - `TestRelay_StreamData_BeforeJoinAck` → `// E2E-02: STREAM_DATA before JOIN_ACK → PROTOCOL_ERROR (ErrCodeRoutingInvalid)`（若 11-02 已写类似注释，统一为上述措辞）

   **不要**修改测试体逻辑，仅注释。
</action>
<acceptance_criteria>
- `grep -q '11-01-PLAN' .planning/ROADMAP.md` && `grep -q '11-02-PLAN' .planning/ROADMAP.md` 退出 0。
- `grep -c 'E2E-01' pkg/relay/relay_test.go` 输出 ≥ 2；`grep -c 'E2E-02' pkg/relay/relay_test.go` 输出 ≥ 2。
</acceptance_criteria>
<verify>
  <automated>grep -q '11-01-PLAN' .planning/ROADMAP.md && grep -q '11-02-PLAN' .planning/ROADMAP.md && test "$(grep -c 'E2E-01' pkg/relay/relay_test.go)" -ge 2 && test "$(grep -c 'E2E-02' pkg/relay/relay_test.go)" -ge 2</automated>
</verify>
<done>路线图列出两计划；relay 测试文件具备 REQ→测试名映射注释。</done>
</task>

<task type="auto">
<name>Task: 11-VALIDATION.md concrete commands + frontmatter</name>
<files>.planning/phases/11-e2e/11-VALIDATION.md</files>
<read_first>
- `.planning/phases/11-e2e/11-VALIDATION.md` — Per-Task Verification Map, Wave 0
</read_first>
<action>
1. 将 **11-02-01** 行的 **Automated Command** 从占位改为：  
   `go test ./pkg/relay/... -run TestRelay_StreamData_BeforeJoinAck -count=1`；**File Exists** 为 ✅；**Status** 待执行器标为 green 后更新。

2. 确认 **11-01-01..03** 行命令与 `TestRelay_StreamData_Broadcast` / `Unicast` / `UnicastMissingDst` 一致。

3. **Wave 0 Requirements** 两 checkbox：在 **11-02** 合并后标为 `[x]`（若仍缺测试则保持 `[ ]` — **本 plan 依赖 11-02 已完成**，应勾选）。

4. **Per-Task Verification Map**：确认 `11-01-01`～`11-01-03` 行的 **Wave 列为 2**（与 `11-01-PLAN.md` 的 `wave: 2` 及「先执行 11-02 再 11-01」一致）；`11-02-01` 行为 Wave **1**。

5. Frontmatter：将 `status: draft` 改为 `status: complete`（或 `ready`），`nyquist_compliant: true`，`wave_0_complete: true`（当且仅当 Wave 0 项已满足）。

6. **Validation Sign-Off**：将自动化相关 checkbox 标为已完成（与仓库状态一致）。
</action>
<acceptance_criteria>
- `grep -q 'TestRelay_StreamData_BeforeJoinAck' .planning/phases/11-e2e/11-VALIDATION.md` 退出 0。
- `grep 'nyquist_compliant: true' .planning/phases/11-e2e/11-VALIDATION.md` 退出 0。
- `grep '| 01 | 2 |' .planning/phases/11-e2e/11-VALIDATION.md` 至少匹配三行（11-01-01～03 的 Wave 列）。
</acceptance_criteria>
<verify>
  <automated>grep -q 'TestRelay_StreamData_BeforeJoinAck' .planning/phases/11-e2e/11-VALIDATION.md && grep -q 'nyquist_compliant: true' .planning/phases/11-e2e/11-VALIDATION.md && test "$(grep -c '| 01 | 2 |' .planning/phases/11-e2e/11-VALIDATION.md)" -ge 3</automated>
</verify>
<done>阶段验证表与 Nyquist 元数据与实现一致。</done>
</task>

</tasks>

<verification>
- `go test ./... -count=1` — 文档变更不应破坏构建；全量测试仍绿（继承 11-02）。
</verification>

<success_criteria>
- 任意审查者可从 REQUIREMENTS → 测试名 → `go test -run` 复现 E2E-01 / E2E-02 证据链。
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e/11-01-SUMMARY.md`
</output>
