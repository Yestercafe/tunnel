---
phase: 11-e2e
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/client/client.go
  - pkg/relay/relay_test.go
autonomous: true
requirements:
  - E2E-02
must_haves:
  truths:
    - "On a real relay.Server TLS connection that has not completed SESSION_JOIN_ACK, a well-formed STREAM_DATA frame elicits MsgTypeProtocolError with framing.ErrCodeRoutingInvalid (reason ERR_ROUTING_INVALID per control.go join gate)."
  artifacts:
    - path: pkg/relay/relay_test.go
      provides: "TestRelay_StreamData_BeforeJoinAck negative integration test"
      contains: "func TestRelay_StreamData_BeforeJoinAck"
    - path: pkg/client/client.go
      provides: "UnderlyingTLSConn() for test-only raw framed writes"
  key_links:
    - from: "pkg/relay/relay_test.go (new test)"
      to: "pkg/relay/control.go MsgTypeStreamData + JoinGateAllowsBusinessDataPlane !ok"
      via: "raw write STREAM_DATA before JoinSession; ReadFrame decodes PROTOCOL_ERROR"
---

<objective>
补齐 **E2E-02** 中 **RESEARCH** 标出的缺口：在**真实** `relay.Server` 上，对**尚未 JOIN**（未收到 `SESSION_JOIN_ACK`）的 TLS 连接发送一帧合法编码的 **`STREAM_DATA`**（绕过 `client.SendStreamData` 的本地 `ErrNotJoined` 门禁），断言 Relay 返回 **`PROTOCOL_ERROR`**，**`err_code == framing.ErrCodeRoutingInvalid`**（与 `pkg/relay/control.go` 中 `EncodeProtocolError(..., \"ERR_ROUTING_INVALID\")` 一致）。

Purpose: 负例落在 Relay 可观测路径，满足「JOIN_ACK 前数据面」字面要求；**非法单播**已由 `TestRelay_StreamData_UnicastMissingDst` 覆盖，本 plan 不重复。
Output: 新测试 `TestRelay_StreamData_BeforeJoinAck` + 最小 `pkg/client` 导出以写入原始帧。
</objective>

<execution_context>
@/Users/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-e2e/11-RESEARCH.md
@pkg/relay/control.go
@pkg/relay/relay_test.go
@pkg/client/client.go
@pkg/framing/frame.go
@pkg/protocol/streamdata.go
@pkg/protocol/join_gate.go

<interfaces>
From `pkg/client/client.go` (existing):
```go
func Dial(ctx context.Context, addr string, tlsCfg *tls.Config) (*Client, error)
func (c *Client) CreateSession(ctx context.Context) (sessionID string, inviteCode string, err error)
func (c *Client) ReadFrame(ctx context.Context) (framing.Frame, error)
func (c *Client) Close() error
```

From `pkg/relay/control.go` (JOIN gate path for `MsgTypeStreamData` when `!st.joined`):
- `protocol.JoinGateAllowsBusinessDataPlane` returns `ok=false` for `MsgTypeStreamData` before join → `protocol.EncodeProtocolError(framing.ErrCodeRoutingInvalid, "ERR_ROUTING_INVALID")`.

Executor will add `UnderlyingTLSConn() *tls.Conn` to mirror `writeFrame` framing (`framing.AppendFrame` + `conn.Write`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
<name>Task: Export UnderlyingTLSConn for raw framed writes</name>
<files>pkg/client/client.go</files>
<read_first>
- `pkg/client/client.go` — `writeFrame`, `Dial`, `Client` struct fields
</read_first>
<action>
Add exported method on `*Client`:

```go
// UnderlyingTLSConn returns the established TLS connection. Intended for integration tests that must send frames not exposed by the public API (e.g. STREAM_DATA before JOIN per Phase 11).
func (c *Client) UnderlyingTLSConn() *tls.Conn
```

Body: `return c.conn` (nil-safe: if `c == nil` or `c.conn == nil`, return nil or document panic — match existing `Close()` style: return `c.conn` only).

Do **not** change `SendStreamData` / `writeFrame` behavior.
</action>
<acceptance_criteria>
- `grep -n 'func (c \*Client) UnderlyingTLSConn' pkg/client/client.go` exits 0.
- `go test ./pkg/client/... -count=1` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -n 'UnderlyingTLSConn' pkg/client/client.go && go test ./pkg/client/... -count=1</automated>
</verify>
<done>`UnderlyingTLSConn` is present and `pkg/client` tests pass.</done>
</task>

<task type="auto" tdd="true">
<name>Task: TestRelay_StreamData_BeforeJoinAck on relay.Server</name>
<files>pkg/relay/relay_test.go</files>
<read_first>
- `pkg/relay/relay_test.go` — `TestRelay_StreamData_UnicastMissingDst` (PROTOCOL_ERROR decode pattern)
- `pkg/relay/control.go` — `case protocol.MsgTypeStreamData` + `!ok` → `ErrCodeRoutingInvalid` / `"ERR_ROUTING_INVALID"`
- `pkg/protocol/streamdata.go` — `EncodeStreamData`, `RoutingPrefix`
</read_first>
<behavior>
- After `CreateSession` only (no `JoinSession`), one framed `STREAM_DATA` (e.g. broadcast, `stream_id=1`, `src_peer_id=0`, app payload `[]byte("prejoin")`) written via `UnderlyingTLSConn` + `framing.AppendFrame` like `writeFrame`.
- Next `ReadFrame` yields `payload[0]==protocol.MsgTypeProtocolError`; `protocol.DecodeProtocolError` gives `code==framing.ErrCodeRoutingInvalid`.
- Test name exactly: `TestRelay_StreamData_BeforeJoinAck`.
</behavior>
<action>
In `pkg/relay/relay_test.go`:

1. Reuse the same harness as `TestRelay_StreamData_Broadcast`: `fakepeer.LocalhostTLSConfig(t)`, `relay.Server{ListenAddr: "127.0.0.1:0", TLSConfig: srvCfg, Registry: relay.NewSessionRegistry()}`, `Listen`, `go srv.Serve(ctx)`, `defer srv.Close()`.

2. `c1, _ := client.Dial(..., addr, tlsClient)`; `defer c1.Close()`.

3. Call `c1.CreateSession` only — **do not** call `JoinSession`.

4. Build `payload, err := protocol.EncodeStreamData(protocol.RoutingPrefix{RoutingMode: protocol.RoutingModeBroadcast, SrcPeerID: 0, DstPeerID: 0}, 1, 0, []byte("prejoin"))` — note `EncodeStreamData` sets `payload[0]=MsgTypeStreamData` internally; `RoutingPrefix.MsgType` is not used by that encoder.

5. `wire := framing.AppendFrame(framing.Frame{Version: framing.VersionV1, Capability: 0, Payload: payload})`.

6. `ctx` with timeout (e.g. `5*time.Second`); set write deadline like `client.writeFrame`; `_, err = c1.UnderlyingTLSConn().Write(wire)`.

7. `f, err := c1.ReadFrame(ctx)`; assert `f.Payload[0]==protocol.MsgTypeProtocolError`; `code, reason, err := protocol.DecodeProtocolError(f.Payload)`; `code == framing.ErrCodeRoutingInvalid`; optionally `reason == "ERR_ROUTING_INVALID"` to match `control.go` literally.

8. Add `// E2E-02: JOIN_ACK 前数据面 → Relay PROTOCOL_ERROR (see Phase 11)` above the test function.
</action>
<acceptance_criteria>
- `grep -q 'func TestRelay_StreamData_BeforeJoinAck' pkg/relay/relay_test.go` exits 0.
- **快检（任务级）：** `go test ./pkg/relay/... -run TestRelay_StreamData_BeforeJoinAck -count=1` exits 0.
- **阶段门闸（wave/phase 合并）：** `go test ./... -count=1` exits 0.
</acceptance_criteria>
<verify>
  <automated>grep -q 'func TestRelay_StreamData_BeforeJoinAck' pkg/relay/relay_test.go && go test ./pkg/relay/... -run TestRelay_StreamData_BeforeJoinAck -count=1</automated>
</verify>
<verify scope="phase_gate">
  <automated>go test ./... -count=1</automated>
</verify>
<done>Pre-JOIN STREAM_DATA on real Relay returns stable `PROTOCOL_ERROR` + `ErrCodeRoutingInvalid`.</done>
</task>

</tasks>

<verification>
- `go test ./... -count=1` — full suite green after this plan.
</verification>

<success_criteria>
- `TestRelay_StreamData_BeforeJoinAck` exists and passes; behavior matches `pkg/relay/control.go` join gate for `MsgTypeStreamData` when `!joined`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e/11-02-SUMMARY.md`
</output>
