---
phase: 05-state-machine-errors-security
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - docs/spec/v1/security-assumptions.md
  - docs/spec/v1/README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SEC-01
user_setup: []
must_haves:
  truths:
    - 文档明确 **TLS 在边缘/服务器终止** 时的信任边界：**Relay 进程内**解析为**明文逻辑帧**
    - 文档明确 v1 **不提供端到端加密**；与 REQUIREMENTS **SEC-01** / v2 **SEC-02** 衔接说明
    - 给出简短 **威胁与缓解** 列表（中继可见、证书域、推荐部署方式至少各一条）
  artifacts:
    - path: docs/spec/v1/security-assumptions.md
      provides: SEC-01 威胁模型与假设
      min_lines: 35
    - path: docs/spec/v1/README.md
      provides: 索引指向 security-assumptions.md
  key_links:
    - from: docs/spec/v1/security-assumptions.md
      to: .planning/REQUIREMENTS.md
      via: SEC-01 表述一致
---

<objective>
交付 **SEC-01**：新增短文档 **`security-assumptions.md`**，写清 **TLS 边缘终止**、**中继信任**、**v1 不做 E2E**、**推荐部署**（如隔离、mTLS、应用层加密的指向 v2），满足可审计的「提供 / 不提供」列表。

Purpose: 与 PROJECT/REQUIREMENTS 锁定方向一致；避免实现误读「TLS 就够了」为端到端保证。
Output: security-assumptions.md、README 索引；勾选 **REQUIREMENTS.md** 中 SEC-01。  
**执行顺序：** 本计划在 **wave 3**、**depends_on 05-02**，避免与 **05-01 / 05-02** 并行修改 `README.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-errors-security/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>新建 security-assumptions.md（威胁模型短文档）</name>
  <files>docs/spec/v1/security-assumptions.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/phases/05-state-machine-errors-security/05-RESEARCH.md</read_first>
  <action>新建 `docs/spec/v1/security-assumptions.md`。必须包含：1) **范围**：v1 协议安全假设，非形式化验证。2) **信任边界**：客户端到 **边缘 TLS 终结点** 的机密性/完整性由 **TLS** 与 **证书身份** 保证；**进入 Relay 进程后** 帧为 **明文**。3) **明确不提供**：v1 **无 E2E 加密**；需防中继窃听时引用 **v2 SEC-02** 或 **部署层**（mTLS、网络隔离、应用层加密）。4) **威胁条目（短表）**：例如 **中继管理员/进程可观测明文**、**错误日志泄露**、**误配置证书** — 每条 **一行缓解或接受策略**。5) 文末 `<!-- REQ: SEC-01 -->`。</action>
  <verify>
    <automated>rg -n 'SEC-01|TLS|E2E|端到端|Relay|威胁|边缘|SEC-02' docs/spec/v1/security-assumptions.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/spec/v1/security-assumptions.md`
    - `rg -q 'REQ: SEC-01' docs/spec/v1/security-assumptions.md`
    - `rg -q 'TLS' docs/spec/v1/security-assumptions.md`
    - `rg -q 'Relay' docs/spec/v1/security-assumptions.md`
    - `rg -ni 'E2E|端到端' docs/spec/v1/security-assumptions.md`
  </acceptance_criteria>
  <done>读者能列出 v1 保证与不保证的边界</done>
</task>

<task type="auto">
  <name>README 索引与 REQUIREMENTS 勾选 SEC-01</name>
  <files>docs/spec/v1/README.md, .planning/REQUIREMENTS.md</files>
  <read_first>docs/spec/v1/security-assumptions.md, docs/spec/v1/README.md, .planning/REQUIREMENTS.md</read_first>
  <action>1) 在 `docs/spec/v1/README.md` 索引表新增 **`security-assumptions.md`**（**SEC-01**）。2) 在 `.planning/REQUIREMENTS.md`：将 **`SEC-01`** 行由 **`[ ]`** 改为 **`[x]`**，**Status** 列若存在则更新为 **Complete**（与文件内其它已完成项格式一致）；若 **Traceability** 表中 **SEC-01** 为 Pending，改为 **Complete**。3) 若文档有 **Last updated** 元数据，更新日期为执行日 **2026-03-29**（与 user_info 一致）。</action>
  <verify>
    <automated>rg -n 'security-assumptions|SEC-01' docs/spec/v1/README.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'security-assumptions.md' docs/spec/v1/README.md`
    - `rg -q '\[x\].*\*\*SEC-01\*\*' .planning/REQUIREMENTS.md`
    - `rg -q 'SEC-01.*Phase 5.*Complete' .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>SEC-01 在需求矩阵中为已完成且索引可发现</done>
</task>

</tasks>

<verification>
- [ ] `rg -q 'REQ: SEC-01' docs/spec/v1/security-assumptions.md`
- [ ] `rg -q 'security-assumptions' docs/spec/v1/README.md`
- [ ] `rg 'SEC-01' .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
- SEC-01 有独立短文档且与 REQUIREMENTS 勾选一致
</success_criteria>

<output>
完成后创建 `.planning/phases/05-state-machine-errors-security/05-03-SUMMARY.md`
</output>
