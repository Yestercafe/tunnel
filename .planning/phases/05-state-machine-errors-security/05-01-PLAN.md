---
phase: 05-state-machine-errors-security
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spec/v1/connection-state.md
  - docs/spec/v1/session-state.md
  - docs/spec/v1/README.md
  - docs/spec/v1/transport-binding.md
  - docs/spec/v1/session-create-join.md
  - docs/spec/v1/streams-lifecycle.md
autonomous: true
requirements:
  - STATE-01
user_setup: []
must_haves:
  truths:
    - 规范中区分三层：字节流/成帧（连接级）、会话/成员（session 级）、逻辑流（stream 级），且各层状态集合不混用
    - 连接级状态机覆盖 TRANS-01：半包/粘包、致命错误（如 ERR_FRAME_TOO_LARGE）与「需更多数据」非错误
    - Session 级状态机覆盖 SESSION_CREATE/JOIN 主路径与失败路径语义（含「未 JOIN_ACK 前不得发送数据面路由帧」）
    - 流级状态引用 streams-lifecycle.md，本计划仅交叉引用、不重复 OPEN/DATA/CLOSE 全文
  artifacts:
    - path: docs/spec/v1/connection-state.md
      provides: STATE-01 连接/成帧层状态与事件
      min_lines: 40
    - path: docs/spec/v1/session-state.md
      provides: STATE-01 会话/成员层状态与事件
      min_lines: 40
    - path: docs/spec/v1/README.md
      provides: 索引新增两篇状态文档
  key_links:
    - from: docs/spec/v1/connection-state.md
      to: docs/spec/v1/transport-binding.md
      via: 「成帧解析循环」一致引用
    - from: docs/spec/v1/session-state.md
      to: docs/spec/v1/session-create-join.md
      via: CREATE/JOIN 控制消息序列
    - from: docs/spec/v1/session-state.md
      to: docs/spec/v1/streams-lifecycle.md
      via: 「数据面须在 JOIN_ACK 之后」
---

<objective>
交付 **STATE-01**：将 **连接级（TLS 字节流 + 成帧解析）** 与 **session 级（创建/加入/成员关系）** 的状态、事件、转移写成可维护的规范终稿；**流级**以 **streams-lifecycle.md** 为权威，本阶段只建立交叉引用与分层边界说明。

Purpose: Phase 2–4 行为分散在多文档；终稿避免「半包」与「未加入 session」混在同一枚举。
Output: `connection-state.md`、`session-state.md`、修订索引与轻量交叉链接。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-errors-security/05-RESEARCH.md
@docs/spec/v1/transport-binding.md
@docs/spec/v1/session-create-join.md
@docs/spec/v1/join-credentials.md
@docs/spec/v1/streams-lifecycle.md
@docs/spec/v1/frame-layout.md
</context>

<tasks>

<task type="auto">
  <name>新建 connection-state.md（成帧/TLS 前提）</name>
  <files>docs/spec/v1/connection-state.md</files>
  <read_first>docs/spec/v1/transport-binding.md, docs/spec/v1/frame-layout.md, .planning/phases/05-state-machine-errors-security/05-RESEARCH.md</read_first>
  <action>新建 `docs/spec/v1/connection-state.md`。必须包含：1) **范围声明**：本文件仅描述 **TLS 应用数据已可用** 之后的 **明文字节流** 上的 **成帧解析状态机**（与 `transport-binding.md` 步骤一致，可用表或编号步骤复述，禁止与 session 状态混写）。2) **状态/事件**：至少区分 **等待更多字节（半包）**、**已交付一帧**、**致命协议错误**（引用占位名 `ERR_FRAME_TOO_LARGE` / `ERR_PROTO_VERSION`，并写明 **ERR-01 将在 errors.md 给数值码**）。3) **粘包**：说明一次读入后循环解析直到缓冲区不足。4) **与 session 层边界**：成帧层只产出 **payload 字节**；不解析 `msg_type` 语义。5) 文末 `<!-- REQ: STATE-01 -->`。</action>
  <verify>
    <automated>rg -n 'STATE-01|ERR_FRAME_TOO_LARGE|ERR_PROTO_VERSION|半包|粘包|transport-binding' docs/spec/v1/connection-state.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/spec/v1/connection-state.md`
    - `rg -q 'REQ: STATE-01' docs/spec/v1/connection-state.md`
    - `rg -q 'transport-binding' docs/spec/v1/connection-state.md`
    - `rg -q 'TLS' docs/spec/v1/connection-state.md`
    - `rg -n 'ERR_FRAME_TOO_LARGE|ERR_PROTO_VERSION' docs/spec/v1/connection-state.md`
  </acceptance_criteria>
  <done>连接/成帧层状态机可单独阅读且与 TRANS-01 一致</done>
</task>

<task type="auto">
  <name>新建 session-state.md（会话/成员层）</name>
  <files>docs/spec/v1/session-state.md</files>
  <read_first>docs/spec/v1/session-create-join.md, docs/spec/v1/join-credentials.md, docs/spec/v1/routing-modes.md, docs/spec/v1/connection-state.md</read_first>
  <action>新建 `docs/spec/v1/session-state.md`。必须包含：1) **范围**：单条 peer↔Relay **TLS 连接**上的 **session 成员关系**（非成帧半包）。2) **主路径状态转移**：从「未创建/未加入」→ **SESSION_CREATE 成功** 或 **SESSION_JOIN 成功**；标明 **收到 `SESSION_JOIN_ACK` 且 peer_id 有效** 为 **可发送数据面帧** 的前置条件（引用 `routing-modes.md` / `streams-lifecycle.md`：**未 ACK 前 MUST NOT** 发送带路由前缀的数据面消息）。3) **失败路径**：用文字或表列出 **token/凭证失败**、**session 不存在** 等（占位错误名 `ERR_JOIN_DENIED`、`ERR_SESSION_NOT_FOUND`，指向 Phase 5 **errors.md**）。4) **与流层关系**：**SESSION_* 消息不得携带 stream_id**；流状态见 `streams-lifecycle.md`。5) 文末 `<!-- REQ: STATE-01 -->`。</action>
  <verify>
    <automated>rg -n 'STATE-01|SESSION_JOIN_ACK|ERR_JOIN_DENIED|ERR_SESSION_NOT_FOUND|streams-lifecycle|数据面' docs/spec/v1/session-state.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/spec/v1/session-state.md`
    - `rg -q 'REQ: STATE-01' docs/spec/v1/session-state.md`
    - `rg -q 'SESSION_JOIN_ACK' docs/spec/v1/session-state.md`
    - `rg -q 'streams-lifecycle.md' docs/spec/v1/session-state.md`
    - `rg -q 'ERR_JOIN_DENIED|ERR_SESSION_NOT_FOUND' docs/spec/v1/session-state.md`
  </acceptance_criteria>
  <done>会话层状态机覆盖创建/加入门禁与失败占位，且不与流 opcode 混用</done>
</task>

<task type="auto">
  <name>修订 README、transport-binding、session-create-join、streams 交叉引用</name>
  <files>docs/spec/v1/README.md, docs/spec/v1/transport-binding.md, docs/spec/v1/session-create-join.md, docs/spec/v1/streams-lifecycle.md</files>
  <read_first>docs/spec/v1/connection-state.md, docs/spec/v1/session-state.md, docs/spec/v1/README.md</read_first>
  <action>1) 在 `docs/spec/v1/README.md` 索引表新增 **`connection-state.md`**、**`session-state.md`** 两行（**STATE-01**、风格与现有行一致）。2) 在 `transport-binding.md` 末尾「错误命名（占位）」或新增一小节：**终稿状态机**见 **`connection-state.md`**（一句交叉引用即可，避免重复整段状态机）。3) 在 `session-create-join.md` 适当位置（如「创建与加入序列」后）增加指向 **`session-state.md`** 的一句。4) 在 `streams-lifecycle.md` 开头「范围」段增加一句：**连接/会话层状态**见 **connection-state / session-state**，本文仅 **流** 生命周期。</action>
  <verify>
    <automated>rg -n 'connection-state|session-state' docs/spec/v1/README.md docs/spec/v1/transport-binding.md docs/spec/v1/session-create-join.md docs/spec/v1/streams-lifecycle.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'connection-state.md' docs/spec/v1/README.md`
    - `rg -q 'session-state.md' docs/spec/v1/README.md`
    - `rg -q 'connection-state' docs/spec/v1/transport-binding.md`
    - `rg -q 'session-state' docs/spec/v1/session-create-join.md`
    - `rg -q 'connection-state\|session-state' docs/spec/v1/streams-lifecycle.md || rg -q 'connection-state' docs/spec/v1/streams-lifecycle.md`
  </acceptance_criteria>
  <done>读者从索引与各主文档能导航到两层状态终稿</done>
</task>

</tasks>

<verification>
- [ ] `rg -q 'REQ: STATE-01' docs/spec/v1/connection-state.md docs/spec/v1/session-state.md`
- [ ] `rg -n 'connection-state|session-state' docs/spec/v1/README.md`
- [ ] `go test ./... -count=1`
</verification>

<success_criteria>
- STATE-01 在两篇新文档中有可检索的 REQ 注释与分层边界
- 与现有 transport-binding、session-create-join、streams-lifecycle 无语义冲突
</success_criteria>

<output>
完成后创建 `.planning/phases/05-state-machine-errors-security/05-01-SUMMARY.md`
</output>
