---
phase: 01-tls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spec/v1/frame-layout.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - FRAME-01
user_setup: []
must_haves:
  truths:
    - 文档定义固定帧头最小字段集、字节偏移、字节序与最大/最小帧尺寸规则
    - 文档含至少一个完整帧的十六进制示例（与字段表一致）
  artifacts:
    - docs/spec/v1/frame-layout.md
    - docs/spec/v1/README.md
  key_links:
    - README.md 链到 frame-layout.md
---

<objective>
撰写 **FRAME-01**：隧道协议 **二进制帧布局** 规范（字段顺序、字节序、长度与边界），并建立 `docs/spec/v1/` 索引页。

Purpose: Phase 1 的物理基础；后续版本号、能力位、传输绑定均依赖本布局。

Output: `docs/spec/v1/frame-layout.md`、`docs/spec/v1/README.md`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tls/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>创建 v1 规范目录与索引</name>
  <files>docs/spec/v1/README.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-tls/01-RESEARCH.md</read_first>
  <action>创建目录 `docs/spec/v1/`。新建 `docs/spec/v1/README.md`，内容须包含：项目名「Tunnel」、本目录为 **v1 协议规范**、列出将存在的文档链接（`frame-layout.md`、`version-capability.md`、`transport-binding.md` 可标为「待补充」或「见各文」）、一句说明「字节序默认为 **大端（big-endian）**」。</action>
  <verify>rg -q 'big-endian|大端' docs/spec/v1/README.md</verify>
  <acceptance_criteria>
    - `docs/spec/v1/README.md` 存在且包含字符串 `frame-layout.md`
    - 文件包含「大端」或 `big-endian`
  </acceptance_criteria>
  <done>索引文件存在且可被后续 PLAN 引用</done>
</task>

<task type="auto">
  <name>撰写帧布局规范 FRAME-01</name>
  <files>docs/spec/v1/frame-layout.md</files>
  <read_first>docs/spec/v1/README.md, .planning/phases/01-tls/01-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>新建 `docs/spec/v1/frame-layout.md`，必须包含以下章节（标题可中英文并列）：1) **范围与术语**；2) **帧结构总览**（ASCII 或表格展示从字节 0 开始的布局）；3) **字段定义表**：每字段名称、偏移、长度（字节）、类型、说明；4) **字节序**：明确全帧数值均为 **big-endian**；5) **长度语义**：明确长度字段表示的是「仅 payload 长度」还是「头+payload」总长度（二选一，推荐与 RESEARCH 一致并说明理由）；6) **最小/最大帧长**（给出具体数字，如最小帧头字节数、最大帧 16MiB 等）；7) **Reserved 字段**：若有，须标为 0 且「语义由后续阶段定义」；8) **示例**：至少一段 **完整帧** 的十六进制 dump（每行 16 字节可选）与字段对照。文档末尾用一行注释块列出 REQ：**FRAME-01**。</action>
  <verify>rg -q 'FRAME-01' docs/spec/v1/frame-layout.md</verify>
  <acceptance_criteria>
    - `docs/spec/v1/frame-layout.md` 包含字符串 `FRAME-01`
    - 文件包含至少 8 个连续十六进制字符对（示例帧）
    - 文件包含「big-endian」或「大端」
  </acceptance_criteria>
  <done>FRAME-01 可在评审中独立于代码实现被验证</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/frame-layout.md`
- [ ] `test -f docs/spec/v1/README.md`
- [ ] `rg FRAME-01 docs/spec/v1/frame-layout.md`
</verification>

<success_criteria>
- FRAME-01 在规范中可测试、可对照十六进制示例
- README 与 frame-layout 互相引用关系清晰
</success_criteria>

<output>
完成后创建 `.planning/phases/01-tls/01-01-SUMMARY.md`
</output>
