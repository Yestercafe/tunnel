---
phase: 01-tls
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - docs/spec/v1/version-capability.md
  - docs/spec/v1/README.md
autonomous: true
requirements:
  - FRAME-02
  - FRAME-03
user_setup: []
must_haves:
  truths:
    - 版本号取值、拒绝未知主版本的规则可执行表述
    - capability 位图中未知位的处理规则无歧义
    - 至少一个含非零 version/capability 的十六进制示例帧
  artifacts:
    - docs/spec/v1/version-capability.md
  key_links:
    - version-capability.md 引用 frame-layout.md 中帧头字段偏移
---

<objective>
撰写 **FRAME-02**（协议版本策略）与 **FRAME-03**（capability 位图与未知位规则），并与 `frame-layout.md` 中的字段对齐。

Purpose: 满足路线图成功标准「版本升级/拒绝策略」「未知 capability 处理」。

Output: `docs/spec/v1/version-capability.md`，并更新 `README.md` 链接状态。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@docs/spec/v1/frame-layout.md
@.planning/phases/01-tls/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>撰写版本与能力规范</name>
  <files>docs/spec/v1/version-capability.md, docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/frame-layout.md, .planning/REQUIREMENTS.md, .planning/phases/01-tls/01-RESEARCH.md</read_first>
  <action>新建 `docs/spec/v1/version-capability.md`。内容必须：1) 在文首用表格列出帧头中 **version** 与 **capability** 字段的**精确字节偏移与宽度**（须与 `frame-layout.md` 一致，若不一致则先修正 frame-layout）；2) **FRAME-02**：定义版本号编码（如 uint16）、**主/次版本**语义或单一递增号；明确 **收到未知主版本** 时：**必须**发送错误帧（错误码占位符名即可）并 **关闭 TLS 连接**；同主版本下 **次版本** 高于己方时可选策略（忽略未知扩展 / 拒绝）；3) **FRAME-03**：定义 capability 为 **uint32 位图**（或文档中与 frame-layout 一致的宽度）；逐位列出 v1 **已分配**位（可为零）；明确 **置位但己方不识别的位** 须 **忽略** 还是 **拒绝**（选一种并给理由）；4) 给出一个 **十六进制示例帧**（含非零 version 或 capability）；5) 文末标注 **FRAME-02**、**FRAME-03**。更新 `docs/spec/v1/README.md`，将 version-capability 标为已存在（若先前为待补充）。</action>
  <verify>rg -q 'FRAME-02' docs/spec/v1/version-capability.md</verify>
  <acceptance_criteria>
    - `version-capability.md` 同时包含字符串 `FRAME-02` 与 `FRAME-03`
    - 文件包含「忽略」或「拒绝」之一与 unknown capability 的明确规则
    - `frame-layout.md` 中 version/capability 偏移与本文一致（执行器需 diff 核对）
  </acceptance_criteria>
  <done>路线图 Phase 1 成功标准 2 与 3 在文档层面可验收</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/version-capability.md`
- [ ] `rg 'FRAME-02|FRAME-03' docs/spec/v1/version-capability.md`
</verification>

<success_criteria>
- 版本与 capability 与帧布局无冲突
- 未知版本/未知位的行为可被实现者直接编码
</success_criteria>

<output>
完成后创建 `.planning/phases/01-tls/01-02-SUMMARY.md`
</output>
