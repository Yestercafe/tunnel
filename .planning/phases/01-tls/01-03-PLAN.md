---
phase: 01-tls
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - docs/spec/v1/transport-binding.md
  - docs/spec/v1/README.md
  - go.mod
  - pkg/framing/decode.go
  - pkg/framing/decode_test.go
autonomous: true
requirements:
  - TRANS-01
user_setup: []
must_haves:
  truths:
    - 规范说明从 TLS 解密后的字节流如何切分为帧（半包/粘包算法文字步骤）
    - 超过最大帧长或头部不完整时的错误处理路径被命名（与后续 ERR 阶段衔接）
  artifacts:
    - docs/spec/v1/transport-binding.md
  key_links:
    - transport-binding 引用 frame-layout 的长度字段定义
---

<objective>
撰写 **TRANS-01**：在 **TLS（TCP）连续字节流** 上将字节解析为 **逻辑帧** 的规则，含粘包/半包；可选加入最小 Go **解码循环** 与测试以验证可读性。

Purpose: 满足路线图「字节流上帧边界、最大帧长、错误恢复」及成功标准 1。

Output: `docs/spec/v1/transport-binding.md`；可选 `pkg/framing` 解码器 + `go test`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@docs/spec/v1/frame-layout.md
@.planning/phases/01-tls/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>撰写 TLS 字节流绑定 TRANS-01</name>
  <files>docs/spec/v1/transport-binding.md, docs/spec/v1/README.md</files>
  <read_first>docs/spec/v1/frame-layout.md, .planning/REQUIREMENTS.md</read_first>
  <action>新建 `docs/spec/v1/transport-binding.md`。必须包含：1) **前提**：连接已为 **TLS 之上** 的字节流（`crypto/tls` 或等价）；2) **解析状态机**：从空缓冲区开始，描述 **读入 → 尝试解析固定头 → 根据长度读 payload → 交付完整帧 → 循环**；3) **半包**：头或 payload 未读满时 **保留缓冲区、等待更多字节**；4) **粘包**：一次读入含多帧时 **while 可解析则连续输出多帧**；5) **最大帧长**：与 `frame-layout.md` 一致，超限行为命名为例如 `ERR_FRAME_TOO_LARGE`（占位）；6) **与版本/capability 关系**：首帧或每帧校验 version 的规则（引用 version-capability.md）；7) 文末 **TRANS-01**。更新 `README.md` 标明 transport-binding 已完成。</action>
  <verify>rg -q 'TRANS-01' docs/spec/v1/transport-binding.md</verify>
  <acceptance_criteria>
    - `transport-binding.md` 包含 `TRANS-01`
    - 文件同时讨论半包与粘包（或英文 `partial`/`multiple frames`）
    - 文件提及 TLS 或「加密字节流」
  </acceptance_criteria>
  <done>TRANS-01 可被独立实现者按文档写 `ReadFrame` 循环</done>
</task>

<task type="auto">
  <name>可选：最小 Go 解码包与 round-trip 测试</name>
  <files>go.mod, pkg/framing/decode.go, pkg/framing/decode_test.go</files>
  <read_first>docs/spec/v1/frame-layout.md, docs/spec/v1/transport-binding.md</read_first>
  <action>若仓库尚无 `go.mod`：运行 `go mod init`（模块路径使用 `github.com/用户/tunnel` 或模块名 `tunnel`，与执行时仓库一致）。在 `pkg/framing/` 实现 **Decode** 或 `ReadFrame(buf []byte)`（返回消耗字节数、帧结构体或错误），行为与 **frame-layout + transport-binding** 一致（至少支持文档中的示例帧十六进制）。`decode_test.go` 中：将示例十六进制解码为结构再编码回字节，**assert 相等**。若项目坚持「本阶段零代码」，则跳过本任务并在 SUMMARY 写「跳过原因」。</action>
  <verify>go test ./pkg/framing/... || true</verify>
  <acceptance_criteria>
    - 若执行代码路径：`go test ./pkg/framing/...` 退出码 0
    - 若跳过：`01-03-SUMMARY.md` 含「跳过 Go 实现」
  </acceptance_criteria>
  <done>文档与可选实现一致</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/spec/v1/transport-binding.md`
- [ ] `rg TRANS-01 docs/spec/v1/transport-binding.md`
</verification>

<success_criteria>
- 任一读实现者可按 transport-binding 实现缓冲解析
- 可选测试通过或明确跳过
</success_criteria>

<output>
完成后创建 `.planning/phases/01-tls/01-03-SUMMARY.md`
</output>
