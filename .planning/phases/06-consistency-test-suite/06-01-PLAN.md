---
phase: 06-consistency-test-suite
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/README.md
  - testdata/framing/.gitkeep
  - testdata/appenvelope/.gitkeep
  - .github/workflows/go.yml
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - 仓库根存在 `testdata/` 子目录，且文档说明与 `06-CONTEXT.md` D-01（framing / appenvelope 分层、`.hex`/`.bin`）一致
    - CI 在 push / pull_request 上运行 `go test ./... -count=1`，Go 版本与 `go.mod` 的 `go 1.22` 一致
  artifacts:
    - path: testdata/README.md
      provides: golden 向量目录约定与命名规则
      min_lines: 25
    - path: .github/workflows/go.yml
      provides: Go 测试工作流
      min_lines: 15
  key_links:
    - from: testdata/README.md
      to: docs/spec/v1/frame-layout.md
      via: framing 向量应对齐 FRAME-01
---

<objective>
建立 **Phase 6 测试载体与 CI 骨架**：`testdata/` 目录分层与命名约定（书面）；**最小** GitHub Actions workflow，使 `go test ./... -count=1` 在 PR 上可重复执行。本计划**不**要求写完所有 golden 用例（留给 06-02）。

Purpose: 满足 TEST-01 中「`testdata/` 或等价」与「`go test` 可运行」的基础设施前提。
Output: `testdata/README.md`、空目录占位、`.github/workflows/go.yml`。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-consistency-test-suite/06-CONTEXT.md
@.planning/phases/06-consistency-test-suite/06-RESEARCH.md
@go.mod
</context>

<tasks>

<task type="auto">
  <name>新增 testdata 目录与 README 约定</name>
  <files>testdata/README.md, testdata/framing/.gitkeep, testdata/appenvelope/.gitkeep</files>
  <read_first>.planning/phases/06-consistency-test-suite/06-CONTEXT.md, go.mod</read_first>
  <action>1) 在仓库根创建 `testdata/framing/`、`testdata/appenvelope/`（可用 `.gitkeep` 占位空目录）。2) 新建 `testdata/README.md`，**必须**用中文或中英对照写明：**子目录用途**（`framing` = 成帧层 golden；`appenvelope` = 应用信封切分）；**文件扩展名**：`.hex` = 十六进制文本（允许空格换行，测试侧 `strings.Fields` 拼接后 `hex.DecodeString`）、`.bin` = 原始二进制；**命名建议**：`{scenario}_{want}.hex`（如 `empty_payload_ok.hex`、`frame_too_large_prefix.err.hex`）；**引用**：指向 `docs/spec/v1/frame-layout.md`、`docs/spec/v1/app-envelope.md`。3) 文首一行注明 **REQ TEST-01** 可追溯。</action>
  <verify>
    <automated>rg -n 'testdata|\.hex|TEST-01|frame-layout|app-envelope' testdata/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f testdata/README.md`
    - `test -f testdata/framing/.gitkeep`
    - `test -f testdata/appenvelope/.gitkeep`
    - `rg -q 'TEST-01' testdata/README.md`
    - `rg -q 'framing' testdata/README.md`
    - `rg -q 'appenvelope' testdata/README.md`
  </acceptance_criteria>
  <done>下游 06-02 可按 README 放置向量文件</done>
</task>

<task type="auto">
  <name>添加 GitHub Actions go.yml</name>
  <files>.github/workflows/go.yml</files>
  <read_first>go.mod, .planning/phases/06-consistency-test-suite/06-CONTEXT.md</read_first>
  <action>新建 `.github/workflows/go.yml`：**触发器** `push` 与 `pull_request`（分支 `main` 与 `master` 若需可并列）；**job** 单节点 `ubuntu-latest`；**步骤**：`actions/checkout@v4`；安装 **Go 1.22**（与 `go.mod` 中 `go 1.22` 一致，使用 `actions/setup-go@v5` 且 `go-version: '1.22'`）；运行 **`go test ./... -count=1`**。不要添加 `-race`（CONTEXT D-08）。文件内注释可写「Phase 6 / TEST-01」。</action>
  <verify>
    <automated>rg -n 'go test|1\.22|setup-go|pull_request' .github/workflows/go.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/go.yml`
    - `rg -q '1\.22' .github/workflows/go.yml`
    - `rg -q 'go test ./...' .github/workflows/go.yml`
    - `rg -q 'pull_request' .github/workflows/go.yml`
  </acceptance_criteria>
  <done>CI 与本地 `go test ./...` 命令对齐</done>
</task>

</tasks>

---

## PLANNING COMPLETE
