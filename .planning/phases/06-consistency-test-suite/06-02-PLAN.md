---
phase: 06-consistency-test-suite
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - pkg/framing/decode_test.go
  - pkg/framing/errors_test.go
  - pkg/appenvelope/split_test.go
  - testdata/framing/*.hex
  - testdata/appenvelope/*.hex
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - `pkg/framing` 覆盖：往返、ErrNeedMore、ErrFrameTooLarge、ErrProtoVersion；`ErrCode` 与 `docs/spec/v1/errors.md` 一致（延伸现有测试）
    - `pkg/appenvelope` 增加至少一组 **testdata 驱动**用例（HAS_APP_ENVELOPE / 边界违反）
    - 至少 **一组** 来自 `docs/spec/v1/routing-modes.md` 广播示例的 **完整帧** golden（ParseFrame 成功 + payload 长度与前缀断言）
    - `go test ./... -count=1` 通过
  artifacts:
    - path: testdata/framing/
      provides: 多组成帧 golden / 负例文件
      min_lines: 0
    - path: testdata/appenvelope/
      provides: 信封切分向量
      min_lines: 0
  key_links:
    - from: pkg/framing/decode_test.go
      to: docs/spec/v1/routing-modes.md
      via: 端到端示例帧注释引用
---

<objective>
在 **06-01** 约定之上，落地 **最小闭环回归集**：**表驱动 / 文件驱动**测试覆盖 `pkg/framing` 与 `pkg/appenvelope`；**少量**规范文档整帧 hex 作为 golden；不引入 testify；不实现 Relay 全状态机。

Purpose: 满足 **TEST-01** 在本里程碑的**收窄解释**：与 **ROADMAP Phase 6 成功标准**一致，覆盖 **关键解析（成帧、信封切分）、路由模式示例帧、错误码/解析错误路径**；**不**在本阶段对「连接/session 状态机」做可执行单元测试（该语义在 Phase 5 规范文档中冻结，可执行验证留待后续若有 `pkg/...` 状态实现时再补）。  
Output: 更新的 `*_test.go`、`testdata/**/*.hex`（或 `.bin`）。
</objective>

<execution_context>
@/home/ivan/repos/tunnel/.cursor/get-shit-done/workflows/execute-plan.md
@/home/ivan/repos/tunnel/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-consistency-test-suite/06-CONTEXT.md
@.planning/phases/06-consistency-test-suite/06-RESEARCH.md
@docs/spec/v1/frame-layout.md
@docs/spec/v1/routing-modes.md
@docs/spec/v1/streams-lifecycle.md
@docs/spec/v1/app-envelope.md
@docs/spec/v1/errors.md
@pkg/framing/decode.go
@pkg/framing/errors.go
@pkg/appenvelope/split.go
</context>

<tasks>

<task type="auto">
  <name>framing：testdata 驱动往返与边界错误</name>
  <files>pkg/framing/decode_test.go, testdata/framing/empty_payload.hex, testdata/framing/three_byte_payload.hex, testdata/framing/payload_too_large_prefix.hex, testdata/framing/wrong_version.hex, testdata/framing/need_more_short.hex</files>
  <read_first>pkg/framing/decode.go, pkg/framing/decode_test.go, docs/spec/v1/frame-layout.md, testdata/README.md</read_first>
  <action>1) 将 `decode_test.go` 中 **`TestRoundTrip_EmptyPayload`** 与 **`TestRoundTrip_ThreeBytePayload`** 使用的 hex 字符串**落盘**为 `testdata/framing/empty_payload.hex` 与 `testdata/framing/three_byte_payload.hex`（内容：与当前测试相同的连续十六进制字符，可每 16 字节换行便于阅读）。2) 修改测试：用 `os.ReadFile` + 去空白 + `hex.DecodeString` 读入，**保持**对 `ParseFrame` / `AppendFrame` 的断言与现在一致。3) 新增表驱动或子测试：**`need_more`** — 输入为 `testdata/framing/need_more_short.hex` 解码后取 **前 5 字节**（或少于 10 字节），期望 `errors.Is(err, ErrNeedMore)`；**`frame_too_large`** — 与现有 `TestErrFrameTooLarge` 一致，可将前缀写入 `testdata/framing/payload_too_large_prefix.hex`（仅 header 10 字节，`payload_len = MaxPayloadLen+1`）；**`proto_version`** — `testdata/framing/wrong_version.hex` 为完整一帧，版本字段 **≠ 0x0001**（其余合法），期望 `ErrProtoVersion`。4) 每个新测试函数注释中写明 **docs** 锚点（`frame-layout.md` 或 `errors.md`）。</action>
  <verify>
    <automated>go test ./pkg/framing/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/framing/... -count=1` 退出码 0
    - `test -f testdata/framing/empty_payload.hex`
    - `test -f testdata/framing/three_byte_payload.hex`
    - `rg -q 'ErrNeedMore|need_more' pkg/framing/decode_test.go`
    - `rg -q 'ErrProtoVersion|wrong_version|proto_version' pkg/framing/decode_test.go`
  </acceptance_criteria>
  <done>成帧层 golden 与半包/版本/过大错误路径有文件可追溯</done>
</task>

<task type="auto">
  <name>framing：routing-modes 广播整帧 golden</name>
  <files>testdata/framing/broadcast_stream_data_routing.hex, pkg/framing/decode_test.go</files>
  <read_first>docs/spec/v1/routing-modes.md, pkg/framing/decode.go, pkg/framing/decode_test.go</read_first>
  <action>1) 从 `docs/spec/v1/routing-modes.md` **「完整逻辑帧示例（广播）」** 复制两行十六进制，**去掉空格** 拼成单一字节序列，写入 `testdata/framing/broadcast_stream_data_routing.hex`（可用换行分组，与 README 约定一致）。2) 在 `decode_test.go` 新增测试（如 `TestGolden_RoutingModes_Broadcast`）：读文件 decode → `ParseFrame` → **必须** `err == nil`；`len(f.Payload) == 22`；`f.Payload[0] == 0x11`（STREAM_DATA）；`f.Payload[1] == 0x01`（BROADCAST）；`binary.BigEndian.Uint64(f.Payload[2:10]) == 0xab`（与文档示例 `src_peer_id`）。3) 测试文件顶部确保 `import` 含 `encoding/binary`（若新增）。4) 注释引用 **`routing-modes.md` 章节名**。</action>
  <verify>
    <automated>go test ./pkg/framing/... -count=1 -run Golden -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f testdata/framing/broadcast_stream_data_routing.hex`
    - `rg -q 'broadcast_stream_data_routing|routing-modes' pkg/framing/decode_test.go`
    - `go test ./pkg/framing/... -count=1 -run Golden` 退出码 0
  </acceptance_criteria>
  <done>第三层「路由 + 流 opcode」至少一条字节级 golden 落地</done>
</task>

<task type="auto">
  <name>appenvelope：testdata 驱动 SplitApplicationData</name>
  <files>pkg/appenvelope/split_test.go, testdata/appenvelope/split_cases.hextxt</files>
  <read_first>pkg/appenvelope/split.go, pkg/appenvelope/split_test.go, docs/spec/v1/app-envelope.md, testdata/README.md</read_first>
  <action>1) 定义一种 **简单行格式**（写入 `testdata/appenvelope/split_cases.hextxt`），例如每行：`# comment` 或 `flags_hex\tapp_hex\twant_err\twant_env_hex\twant_body_hex`（全小写 hex，空用 `-`）；至少 **3 行有效数据**：对应现有用例「无信封 body」「envelope len 2 + body」「truncated」之一组。2) 在 `split_test.go` 新增 `TestSplitApplicationData_FileDriven`：解析该文件，对每行调用 `SplitApplicationData`，`errors.Is` 与 bytes 比较与表内期望一致。3) **保留**原有 `TestSplitApplicationData` 表驱动不删除（双轨）。4) 文件头注释引用 **app-envelope.md**。5) **同步**更新 `testdata/README.md`：在「文件扩展名」小节增加 **`.hextxt`**：行分隔的文本驱动用例（仅 appenvelope 分割测试），与 `.hex`（纯十六进制字节）区分。</action>
  <verify>
    <automated>go test ./pkg/appenvelope/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f testdata/appenvelope/split_cases.hextxt`
    - `rg -q 'split_cases|FileDriven' pkg/appenvelope/split_test.go`
    - `rg -q '\.hextxt' testdata/README.md`
    - `go test ./pkg/appenvelope/... -count=1` 退出码 0
  </acceptance_criteria>
  <done>信封边界有 testdata 可追溯，满足 CONTEXT D-05</done>
</task>

<task type="auto">
  <name>全量测试与 ErrCode 一致性抽检</name>
  <files>pkg/framing/errors_test.go</files>
  <read_first>docs/spec/v1/errors.md, pkg/framing/errors.go, pkg/framing/errors_test.go</read_first>
  <action>1) 运行 **`go test ./... -count=1`**，修复直至通过。2) 若 `errors_test.go` 已覆盖全部 `ErrCode` 与文档表，**仅**在注释中增加一行「Phase 6：与 errors.md 对照无新增码」；若文档有新增码而 Go 未定义，则在本任务内 **补齐常量与测试**（与 Phase 5 风格一致）。3) 不强制 `PROTOCOL_ERROR` 编码 API，除非为通过某测试所必需（CONTEXT D-09）。</action>
  <verify>
    <automated>go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` 退出码 0
    - `rg -q 'ErrCode|errors.md' pkg/framing/errors_test.go`
  </acceptance_criteria>
  <done>仓库级测试绿，TEST-01 可验收</done>
</task>

</tasks>

---

## PLANNING COMPLETE
